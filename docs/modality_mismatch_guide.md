# Physics World Model — Modality Mismatch Guide

> Auto-generated by `scripts/test_mismatch_examples.py`. Do not edit manually.

This document shows, for each of the 64 PWM imaging modalities, what goes wrong when the **widefield Gaussian blur fallback** (sigma=2.0, shape-preserving, real-valued, self-adjoint) is used instead of the correct physics operator.

## Overview

| # | Modality | Category | Build | Mismatch Types | Ex1 Shape Match | Ex1 RMSE | Ex2 RMSE |
|---|----------|----------|-------|----------------|-----------------|----------|----------|
| 1 | `widefield` | microscopy | OK | none | Yes | 0.0000 | 0.9800 |
| 2 | `widefield_lowdose` | microscopy | OK | content | Yes | 0.0000 | 0.9800 |
| 3 | `confocal_livecell` | microscopy | OK | content | Yes | 0.1346 | 0.9496 |
| 4 | `confocal_3d` | microscopy | OK | dimensional, content | **No** (32, 64, 64) vs (64, 64) | N/A | 0.9998 |
| 5 | `sim` | microscopy | OK | shape, content | **No** (64, 64, 9) vs (64, 64) | N/A | 0.9833 |
| 6 | `lightsheet` | microscopy | OK | dimensional, content | **No** (64, 64, 32) vs (64, 64) | N/A | 0.9984 |
| 7 | `cassi` | compressive | OK | shape, dimensional, content | Yes | 1.7780 | N/A |
| 8 | `spc` | compressive | OK | shape, content | **No** (614) vs (64, 64) | N/A | 0.9273 |
| 9 | `cacti` | compressive | OK | dimensional, content | Yes | 2.0146 | 1.5020 |
| 10 | `matrix` | compressive | OK | shape, content | Yes | 0.0000 | 0.9800 |
| 11 | `ct` | medical | OK | shape, content | **No** (180, 64) vs (64, 64) | N/A | 1.6748 |
| 12 | `mri` | medical | OK | domain, content | Yes | 35.7292 | 1.1605 |
| 13 | `ptychography` | coherent | OK | shape, nonlinear, content | **No** (16, 32, 32) vs (64, 64) | N/A | 1.0242 |
| 14 | `holography` | coherent | OK | domain, content | Yes | 2.8429 | 1.1029 |
| 15 | `phase_retrieval` | coherent | OK | shape, nonlinear, content | Yes | 5759.2899 | 1.4056 |
| 16 | `fpm` | coherent | OK | shape, nonlinear, content | **No** (25, 16, 16) vs (64, 64) | N/A | 1.0002 |
| 17 | `nerf` | neural_rendering | OK | dimensional, nonlinear, shape, content | **No** (10, 64, 64) vs (64, 64) | N/A | 0.9922 |
| 18 | `gaussian_splatting` | neural_rendering | OK | dimensional, nonlinear, shape, content | **No** (10, 64, 64) vs (64, 64) | N/A | 1.0032 |
| 19 | `lensless` | computational | OK | content | Yes | 0.9600 | 0.0057 |
| 20 | `panorama` | computational | OK | content | Yes | 0.2596 | 0.9324 |
| 21 | `light_field` | computational | OK | shape, content | **No** (64, 64) vs (64, 5) | N/A | 1.0022 |
| 22 | `dot` | medical | OK | shape, content | **No** (64) vs (64, 64) | N/A | 1.0013 |
| 23 | `photoacoustic` | medical | OK | shape, content | **No** (32, 91) vs (64, 64) | N/A | 75.5043 |
| 24 | `oct` | medical | OK | content | **No** (64, 128) vs (64, 64) | N/A | 259.7463 |
| 25 | `flim` | microscopy | OK | shape, nonlinear, content | **No** (64, 64, 64) vs (64, 64) | N/A | 1.4784 |
| 26 | `integral` | computational | OK | shape, content | Yes | 0.1420 | 1.0038 |
| 27 | `xray_radiography` | medical | OK | content | Yes | 0.5981 | 0.6191 |
| 28 | `ultrasound` | medical | OK | shape, content | **No** (32, 128) vs (64, 64) | N/A | 976.2694 |
| 29 | `pet` | medical | OK | shape, content | **No** (32, 64) vs (64, 64) | N/A | 57.0719 |
| 30 | `spect` | medical | OK | shape, content | **No** (32, 64) vs (64, 64) | N/A | 45.9173 |
| 31 | `sem` | electron_microscopy | OK | content | Yes | 0.1009 | 0.9918 |
| 32 | `tem` | electron_microscopy | OK | domain, content | Yes | 0.1735 | 0.9914 |
| 33 | `electron_tomography` | electron_microscopy | OK | dimensional, shape, content | **No** (16, 64, 64) vs (64, 64) | N/A | 22.4370 |
| 34 | `stem` | electron_microscopy | OK | content | Yes | 0.1595 | N/A |
| 35 | `fluoroscopy` | medical | OK | content | Yes | 9388.9027 | N/A |
| 36 | `mammography` | medical | OK | content | Yes | 18241.2618 | N/A |
| 37 | `dexa` | medical | OK | shape, content | **No** (2, 64, 64) vs (64, 64) | N/A | N/A |
| 38 | `cbct` | medical | OK | shape, content | **No** (180, 64) vs (64, 64) | N/A | N/A |
| 39 | `angiography` | medical | OK | content | Yes | 12071.4407 | N/A |
| 40 | `doppler_ultrasound` | medical | OK | shape, content | **No** (32, 64) vs (64, 64) | N/A | N/A |
| 41 | `elastography` | medical | OK | shape, content | **No** (32, 64) vs (64, 64) | N/A | N/A |
| 42 | `fmri` | medical | OK | content | Yes | 15.6714 | N/A |
| 43 | `mrs` | medical | OK | content | Yes | 4.7578 | N/A |
| 44 | `diffusion_mri` | medical | OK | content | Yes | 38.2974 | N/A |
| 45 | `two_photon` | microscopy | OK | content | Yes | 0.5101 | N/A |
| 46 | `sted` | microscopy | OK | content | Yes | 0.1413 | N/A |
| 47 | `palm_storm` | microscopy | OK | content | Yes | 210.4934 | N/A |
| 48 | `tirf` | microscopy | OK | content | Yes | 0.1441 | N/A |
| 49 | `polarization` | microscopy | OK | content | Yes | 0.1199 | N/A |
| 50 | `endoscopy` | clinical_optics | OK | shape, content | **No** (4096) vs (64, 64) | N/A | N/A |
| 51 | `fundus` | clinical_optics | OK | content | Yes | 0.1232 | N/A |
| 52 | `octa` | clinical_optics | OK | content | Yes | 0.4745 | N/A |
| 53 | `tof_camera` | depth_imaging | OK | content | Yes | 0.2418 | N/A |
| 54 | `lidar` | depth_imaging | OK | shape, content | **No** (64) vs (64, 64) | N/A | N/A |
| 55 | `structured_light` | depth_imaging | OK | content | Yes | 0.3545 | N/A |
| 56 | `sar` | remote_sensing | OK | content | Yes | 6.2954 | N/A |
| 57 | `sonar` | remote_sensing | OK | shape, content | **No** (64) vs (64, 64) | N/A | N/A |
| 58 | `electron_diffraction` | electron_microscopy | OK | content | Yes | 3868.4630 | N/A |
| 59 | `ebsd` | electron_microscopy | OK | content | Yes | 0.2938 | N/A |
| 60 | `eels` | electron_microscopy | OK | content | Yes | 0.4893 | N/A |
| 61 | `electron_holography` | electron_microscopy | OK | domain, content | Yes | 0.4275 | N/A |
| 62 | `neutron_tomo` | particle_imaging | OK | content | Yes | 5017.3274 | N/A |
| 63 | `proton_radiography` | particle_imaging | OK | content | Yes | 10010.4943 | N/A |
| 64 | `muon_tomo` | particle_imaging | OK | content | Yes | 0.1193 | N/A |

## Mismatch Categories

| Type | Description | Count |
|------|-------------|-------|
| **shape** | Correct output shape differs from (64,64) | 26 |
| **domain** | Complex output vs real fallback | 4 |
| **content** | Same shape but fundamentally wrong physics values | 63 |
| **nonlinear** | Linear fallback vs nonlinear correct model | 6 |
| **dimensional** | 3D input required vs 2D fallback | 7 |

---

## 1. Widefield Fluorescence Microscopy (`widefield`)

**Physics**: Standard widefield epi-fluorescence microscopy where the entire field of view is illuminated simultaneously and the image is formed by convolution of the specimen fluorescence distribution with the system point spread function (PSF). Out-of-focus blur is the primary degradation.

**Forward equation**: `y = PSF ** x + n`

**Correct operator**: `dedicated` | **Input shape**: (64, 64) | **Output shape**: (64, 64) | **Linear**: Yes

### Mismatch Example 1: Forward Pass Comparison

| Metric | Correct | Widefield Fallback |
|--------|---------|-------------------|
| Output shape | (64, 64) | (64, 64) |
| Shapes match | Yes | — |
| Complex output | No | No |
| RMSE | 0.000000 | — |
| Correlation | 1.0000 | — |
| Max abs diff | 0.000000 | — |
| Relative RMSE | 0.0000 | — |

**Failure**: Minimal mismatch — the widefield fallback closely matches this modality.

**Correction**: The widefield fallback is acceptable for this modality.

### Mismatch Example 2: Round-Trip Fidelity

| Metric | Correct | Widefield Fallback |
|--------|---------|-------------------|
| Output shape | (64, 64) | (64, 64) |
| Shapes match | Yes | — |
| Round-trip RMSE | 0.979956 | — |

**Failure**: Round-trip fidelity is similar — minimal mismatch.

**Correction**: The widefield round-trip is acceptable for this modality.

---

## 2. Low-Dose Widefield Microscopy (`widefield_lowdose`)

**Physics**: Widefield fluorescence microscopy operated at very low illumination power or short exposure time to reduce phototoxicity. Images are dominated by shot noise and read noise. The PSF model is identical to standard widefield but the noise model differs significantly (heavy Poisson + Gaussian).

**Forward equation**: `y = Poisson(alpha * PSF ** x) / alpha + N(0, sigma^2)`

**Correct operator**: `dedicated` | **Input shape**: (64, 64) | **Output shape**: (64, 64) | **Linear**: Yes

### Mismatch Example 1: Forward Pass Comparison

| Metric | Correct | Widefield Fallback |
|--------|---------|-------------------|
| Output shape | (64, 64) | (64, 64) |
| Shapes match | Yes | — |
| Complex output | No | No |
| RMSE | 0.000000 | — |
| Correlation | 1.0000 | — |
| Max abs diff | 0.000000 | — |
| Relative RMSE | 0.0000 | — |

**Failure**: Minimal mismatch — the widefield fallback closely matches this modality.

**Correction**: The widefield fallback is acceptable for this modality.

### Mismatch Example 2: Round-Trip Fidelity

| Metric | Correct | Widefield Fallback |
|--------|---------|-------------------|
| Output shape | (64, 64) | (64, 64) |
| Shapes match | Yes | — |
| Round-trip RMSE | 0.979956 | — |

**Failure**: Round-trip fidelity is similar — minimal mismatch.

**Correction**: The widefield round-trip is acceptable for this modality.

---

## 3. Confocal Live-Cell Microscopy (`confocal_livecell`)

**Physics**: Laser scanning confocal microscopy for live-cell imaging. A focused laser scans the specimen point by point, and a pinhole rejects out-of-focus light. The confocal PSF is sharper (sigma~1.2-1.5) than widefield (sigma=2.0), producing fundamentally different blur characteristics.

**Forward equation**: `y = PSF_confocal ** x + n`

**Correct operator**: `graph` | **Input shape**: (64, 64) | **Output shape**: (64, 64) | **Linear**: Yes

### Mismatch Example 1: Forward Pass Comparison

| Metric | Correct | Widefield Fallback |
|--------|---------|-------------------|
| Output shape | (64, 64) | (64, 64) |
| Shapes match | Yes | — |
| Complex output | No | No |
| RMSE | 0.134567 | — |
| Correlation | 0.8890 | — |
| Max abs diff | 0.516222 | — |
| Relative RMSE | 0.5366 | — |

**Failure**: While output shapes match, the pixel values are fundamentally different. RMSE = 0.134567, correlation = 0.8890. The widefield blur applies a generic Gaussian PSF that does not capture the physics of Confocal Live-Cell Microscopy.

**Correction**: Use the correct `confocal_livecell_graph_v1` operator which implements the physical forward model: y = PSF_confocal ** x + n.

### Mismatch Example 2: Round-Trip Fidelity

| Metric | Correct | Widefield Fallback |
|--------|---------|-------------------|
| Output shape | (64, 64) | (64, 64) |
| Shapes match | Yes | — |
| Round-trip RMSE | 0.949585 | — |

**Failure**: Correct round-trip RMSE = 0.949585, fallback round-trip RMSE = 0.979956. The widefield round-trip loses structure-specific information that the correct operator preserves.

**Correction**: Use the paired `confocal_livecell_graph_v1` forward/adjoint for proper forward modeling and reconstruction.

---

## 4. Confocal 3D Z-Stack (`confocal_3d`)

**Physics**: Three-dimensional confocal imaging by acquiring a z-stack of optical sections. Each slice is convolved with the 3D confocal PSF. The anisotropic PSF (worse axial resolution) and volumetric data are key differences from 2D widefield. The correct operator works on 3D volumes, not 2D images.

**Forward equation**: `y(x,y,z) = PSF_3d *** x(x,y,z) + n`

**Correct operator**: `graph` | **Input shape**: (32, 64, 64) | **Output shape**: (32, 64, 64) | **Linear**: Yes

### Mismatch Example 1: Forward Pass Comparison

| Metric | Correct | Widefield Fallback |
|--------|---------|-------------------|
| Output shape | (32, 64, 64) | (64, 64) |
| Shapes match | **No** | — |
| Complex output | No | No |

**Failure**: The correct forward model produces output of shape (32, 64, 64), but the widefield fallback produces shape (64, 64). Any reconstruction algorithm expecting the correct output shape will crash or produce meaningless results.

**Correction**: Use the correct `confocal_3d_graph_v1` operator which produces the physically valid output shape (32, 64, 64).

### Mismatch Example 2: Round-Trip Fidelity

| Metric | Correct | Widefield Fallback |
|--------|---------|-------------------|
| Output shape | (32, 64, 64) | (64, 64) |
| Shapes match | **No** | — |
| Round-trip RMSE | 0.999753 | — |

**Failure**: Correct round-trip RMSE = 0.999753, fallback round-trip RMSE = 0.979956. The widefield round-trip loses structure-specific information that the correct operator preserves.

**Correction**: Use the paired `confocal_3d_graph_v1` forward/adjoint for proper forward modeling and reconstruction.

---

## 5. Structured Illumination Microscopy (`sim`)

**Physics**: Structured Illumination Microscopy achieves ~2x lateral resolution improvement by illuminating with sinusoidal patterns at multiple orientations and phases. The forward model produces multiple patterned images (n_angles * n_phases = 9), fundamentally different from a single blurred output.

**Forward equation**: `y_k = PSF ** (I_k * x) + n, k=1..9`

**Correct operator**: `dedicated` | **Input shape**: (64, 64) | **Output shape**: (64, 64, 9) | **Linear**: Yes

### Mismatch Example 1: Forward Pass Comparison

| Metric | Correct | Widefield Fallback |
|--------|---------|-------------------|
| Output shape | (64, 64, 9) | (64, 64) |
| Shapes match | **No** | — |
| Complex output | No | No |

**Failure**: The correct forward model produces output of shape (64, 64, 9), but the widefield fallback produces shape (64, 64). Any reconstruction algorithm expecting the correct output shape will crash or produce meaningless results.

**Correction**: Use the correct `sim` operator which produces the physically valid output shape (64, 64, 9).

### Mismatch Example 2: Round-Trip Fidelity

| Metric | Correct | Widefield Fallback |
|--------|---------|-------------------|
| Output shape | (64, 64) | (64, 64) |
| Shapes match | Yes | — |
| Round-trip RMSE | 0.983313 | — |

**Failure**: Correct round-trip RMSE = 0.983313, fallback round-trip RMSE = 0.979956. The widefield round-trip loses structure-specific information that the correct operator preserves.

**Correction**: Use the paired `sim` forward/adjoint for proper forward modeling and reconstruction.

---

## 6. Light-Sheet Fluorescence Microscopy (`lightsheet`)

**Physics**: Light-sheet (SPIM) illuminates the sample with a thin sheet of light perpendicular to the detection axis, providing intrinsic optical sectioning. The correct operator works on 3D volumes with anisotropic PSF blur and stripe artifacts — a 2D Gaussian blur loses all volumetric information.

**Forward equation**: `y = S(z) * (PSF_3d *** x) + n`

**Correct operator**: `dedicated` | **Input shape**: (64, 64, 32) | **Output shape**: (64, 64, 32) | **Linear**: Yes

### Mismatch Example 1: Forward Pass Comparison

| Metric | Correct | Widefield Fallback |
|--------|---------|-------------------|
| Output shape | (64, 64, 32) | (64, 64) |
| Shapes match | **No** | — |
| Complex output | No | No |

**Failure**: The correct forward model produces output of shape (64, 64, 32), but the widefield fallback produces shape (64, 64). Any reconstruction algorithm expecting the correct output shape will crash or produce meaningless results.

**Correction**: Use the correct `lightsheet` operator which produces the physically valid output shape (64, 64, 32).

### Mismatch Example 2: Round-Trip Fidelity

| Metric | Correct | Widefield Fallback |
|--------|---------|-------------------|
| Output shape | (64, 64, 32) | (64, 64) |
| Shapes match | **No** | — |
| Round-trip RMSE | 0.998446 | — |

**Failure**: Correct round-trip RMSE = 0.998446, fallback round-trip RMSE = 0.976433. The widefield round-trip loses structure-specific information that the correct operator preserves.

**Correction**: Use the paired `lightsheet` forward/adjoint for proper forward modeling and reconstruction.

---

## 7. Coded Aperture Snapshot Spectral Imaging (CASSI) (`cassi`)

**Physics**: CASSI compresses a 3D spectral data cube (H x W x L) into a 2D coded measurement via a binary coded aperture mask and spectral dispersion. The output shape differs from the input due to dispersive shift. The widefield fallback ignores spectral encoding entirely.

**Forward equation**: `y(x,y) = sum_l M(x,y) * X(x, y-s(l), l)`

**Correct operator**: `dedicated` | **Input shape**: (64, 64) | **Output shape**: (64, 64) | **Linear**: Yes

### Mismatch Example 1: Forward Pass Comparison

| Metric | Correct | Widefield Fallback |
|--------|---------|-------------------|
| Output shape | (64, 64) | (64, 64) |
| Shapes match | Yes | — |
| Complex output | No | No |
| RMSE | 1.778001 | — |
| Correlation | 0.1974 | — |
| Max abs diff | 8.336653 | — |
| Relative RMSE | 0.9864 | — |

**Failure**: While output shapes match, the pixel values are fundamentally different. RMSE = 1.778001, correlation = 0.1974. The widefield blur applies a generic Gaussian PSF that does not capture the physics of Coded Aperture Snapshot Spectral Imaging (CASSI).

**Correction**: Use the correct `cassi` operator which implements the physical forward model: y(x,y) = sum_l M(x,y) * X(x, y-s(l), l).

### Mismatch Example 2: Round-Trip Fidelity

| Metric | Correct | Widefield Fallback |
|--------|---------|-------------------|
| Output shape | (64, 64, 8) | (64, 64) |
| Shapes match | **No** | — |

**Failure**: The correct operator's adjoint is not available (non-linear model). The widefield round-trip (blur->blur) simply double-blurs the image, which does not represent the non-linear inverse problem.

**Correction**: For non-linear modalities like Coded Aperture Snapshot Spectral Imaging (CASSI), iterative algorithms (gradient descent, ADMM) must use the correct forward model.

---

## 8. Single-Pixel Camera (`spc`)

**Physics**: The single-pixel camera acquires compressive measurements by projecting the scene onto a sequence of random binary patterns and recording a single scalar intensity per pattern. The output is a 1D measurement vector, not a 2D image — fundamentally incompatible with the widefield 2D blur model.

**Forward equation**: `y = A @ vec(x)`

**Correct operator**: `dedicated` | **Input shape**: (64, 64) | **Output shape**: (614) | **Linear**: Yes

### Mismatch Example 1: Forward Pass Comparison

| Metric | Correct | Widefield Fallback |
|--------|---------|-------------------|
| Output shape | (614) | (64, 64) |
| Shapes match | **No** | — |
| Complex output | No | No |

**Failure**: The correct forward model produces output of shape (614,), but the widefield fallback produces shape (64, 64). Any reconstruction algorithm expecting the correct output shape will crash or produce meaningless results.

**Correction**: Use the correct `spc` operator which produces the physically valid output shape (614,).

### Mismatch Example 2: Round-Trip Fidelity

| Metric | Correct | Widefield Fallback |
|--------|---------|-------------------|
| Output shape | (64, 64) | (64, 64) |
| Shapes match | Yes | — |
| Round-trip RMSE | 0.927341 | — |

**Failure**: Correct round-trip RMSE = 0.927341, fallback round-trip RMSE = 0.979956. The widefield round-trip loses structure-specific information that the correct operator preserves.

**Correction**: Use the paired `spc` forward/adjoint for proper forward modeling and reconstruction.

---

## 9. CACTI (Video Snapshot Compressive Imaging) (`cacti`)

**Physics**: CACTI compresses a video sequence (H x W x T frames) into a single 2D snapshot via time-varying coded apertures. Each temporal frame is modulated by a different mask pattern and all frames are summed on the detector. The widefield fallback has no temporal coding structure.

**Forward equation**: `y(x,y) = sum_t M_t(x,y) * X(x,y,t)`

**Correct operator**: `dedicated` | **Input shape**: (64, 64, 8) | **Output shape**: (64, 64) | **Linear**: Yes

### Mismatch Example 1: Forward Pass Comparison

| Metric | Correct | Widefield Fallback |
|--------|---------|-------------------|
| Output shape | (64, 64) | (64, 64) |
| Shapes match | Yes | — |
| Complex output | No | No |
| RMSE | 2.014648 | — |
| Correlation | 0.0793 | — |
| Max abs diff | 8.437287 | — |
| Relative RMSE | 0.9969 | — |

**Failure**: While output shapes match, the pixel values are fundamentally different. RMSE = 2.014648, correlation = 0.0793. The widefield blur applies a generic Gaussian PSF that does not capture the physics of CACTI (Video Snapshot Compressive Imaging).

**Correction**: Use the correct `cacti` operator which implements the physical forward model: y(x,y) = sum_t M_t(x,y) * X(x,y,t).

### Mismatch Example 2: Round-Trip Fidelity

| Metric | Correct | Widefield Fallback |
|--------|---------|-------------------|
| Output shape | (64, 64, 8) | (64, 64) |
| Shapes match | **No** | — |
| Round-trip RMSE | 1.502037 | — |

**Failure**: Correct round-trip RMSE = 1.502037, fallback round-trip RMSE = 0.970420. The widefield round-trip loses structure-specific information that the correct operator preserves.

**Correction**: Use the paired `cacti` forward/adjoint for proper forward modeling and reconstruction.

---

## 10. Generic Matrix Sensing (`matrix`)

**Physics**: Generic matrix operator where a random measurement matrix A maps the vectorized image to a compressed measurement vector. The output is a 1D vector of length M << N, losing all spatial structure that the widefield blur model preserves.

**Forward equation**: `y = A @ x + n`

**Correct operator**: `dedicated` | **Input shape**: (64, 64) | **Output shape**: (64, 64) | **Linear**: Yes

### Mismatch Example 1: Forward Pass Comparison

| Metric | Correct | Widefield Fallback |
|--------|---------|-------------------|
| Output shape | (64, 64) | (64, 64) |
| Shapes match | Yes | — |
| Complex output | No | No |
| RMSE | 0.000000 | — |
| Correlation | 1.0000 | — |
| Max abs diff | 0.000000 | — |
| Relative RMSE | 0.0000 | — |

**Failure**: Minimal mismatch — the widefield fallback closely matches this modality.

**Correction**: The widefield fallback is acceptable for this modality.

### Mismatch Example 2: Round-Trip Fidelity

| Metric | Correct | Widefield Fallback |
|--------|---------|-------------------|
| Output shape | (64, 64) | (64, 64) |
| Shapes match | Yes | — |
| Round-trip RMSE | 0.979956 | — |

**Failure**: Round-trip fidelity is similar — minimal mismatch.

**Correction**: The widefield round-trip is acceptable for this modality.

---

## 11. X-ray Computed Tomography (`ct`)

**Physics**: X-ray CT acquires line integrals of the attenuation coefficient at multiple angles via the Radon transform. The output is a sinogram of shape (n_angles, n_detectors), fundamentally different from the 2D input. The widefield blur cannot represent angular projections.

**Forward equation**: `y(theta, s) = integral x(r) dl (Radon transform)`

**Correct operator**: `dedicated` | **Input shape**: (64, 64) | **Output shape**: (180, 64) | **Linear**: Yes

### Mismatch Example 1: Forward Pass Comparison

| Metric | Correct | Widefield Fallback |
|--------|---------|-------------------|
| Output shape | (180, 64) | (64, 64) |
| Shapes match | **No** | — |
| Complex output | No | No |

**Failure**: The correct forward model produces output of shape (180, 64), but the widefield fallback produces shape (64, 64). Any reconstruction algorithm expecting the correct output shape will crash or produce meaningless results.

**Correction**: Use the correct `ct` operator which produces the physically valid output shape (180, 64).

### Mismatch Example 2: Round-Trip Fidelity

| Metric | Correct | Widefield Fallback |
|--------|---------|-------------------|
| Output shape | (64, 64) | (64, 64) |
| Shapes match | Yes | — |
| Round-trip RMSE | 1.674789 | — |

**Failure**: Correct round-trip RMSE = 1.674789, fallback round-trip RMSE = 0.979956. The widefield round-trip loses structure-specific information that the correct operator preserves.

**Correction**: Use the paired `ct` forward/adjoint for proper forward modeling and reconstruction.

---

## 12. Magnetic Resonance Imaging (`mri`)

**Physics**: MRI acquires data in the spatial frequency domain (k-space) via the Fourier transform, with undersampling via a binary mask. The output is complex-valued undersampled k-space data — the widefield fallback produces only real-valued spatially blurred output, losing phase information entirely.

**Forward equation**: `y = M * F * x + n (k-space undersampling)`

**Correct operator**: `dedicated` | **Input shape**: (64, 64) | **Output shape**: (64, 64) | **Linear**: Yes

### Mismatch Example 1: Forward Pass Comparison

| Metric | Correct | Widefield Fallback |
|--------|---------|-------------------|
| Output shape | (64, 64) | (64, 64) |
| Shapes match | Yes | — |
| Complex output | Yes | No |
| RMSE | 35.729225 | — |
| Correlation | -0.0510 | — |
| Max abs diff | 172.373960 | — |
| Relative RMSE | 1.0005 | — |

**Failure**: The correct forward model produces complex-valued output (contains phase information), but the widefield fallback produces only real-valued output. Phase information is critical for this modality.

**Correction**: Use the correct `mri` operator which preserves complex-valued output containing both amplitude and phase.

### Mismatch Example 2: Round-Trip Fidelity

| Metric | Correct | Widefield Fallback |
|--------|---------|-------------------|
| Output shape | (64, 64) | (64, 64) |
| Shapes match | Yes | — |
| Round-trip RMSE | 1.160470 | — |

**Failure**: Correct round-trip RMSE = 1.160470, fallback round-trip RMSE = 0.979956. The widefield round-trip loses structure-specific information that the correct operator preserves.

**Correction**: Use the paired `mri` forward/adjoint for proper forward modeling and reconstruction.

### Mismatch Example 3: Domain Mismatch — Complex vs Real Output

| Metric | Correct | Widefield Fallback |
|--------|---------|-------------------|
| Output shape | (64, 64) | (64, 64) |
| Shapes match | Yes | — |

**Failure**: The correct operator output has significant imaginary component (|imag| RMS = 24.788895, |real| RMS = 24.957148). The widefield fallback produces only real-valued output, discarding all phase information.

**Correction**: Use `mri` which correctly handles complex-valued wave propagation and phase contrast.

---

## 13. Ptychographic Imaging (`ptychography`)

**Physics**: Ptychography is a scanning coherent imaging technique where overlapping probe positions illuminate the sample. Each diffraction pattern is the squared modulus of the Fourier transform of the probe-sample product. The non-linear magnitude-squared detection makes this fundamentally different from linear convolution.

**Forward equation**: `I_j = |FFT(P(r) * O(r - r_j))|^2`

**Correct operator**: `dedicated` | **Input shape**: (64, 64) | **Output shape**: (16, 32, 32) | **Linear**: No

### Mismatch Example 1: Forward Pass Comparison

| Metric | Correct | Widefield Fallback |
|--------|---------|-------------------|
| Output shape | (16, 32, 32) | (64, 64) |
| Shapes match | **No** | — |
| Complex output | No | No |

**Failure**: The correct forward model produces output of shape (16, 32, 32), but the widefield fallback produces shape (64, 64). Any reconstruction algorithm expecting the correct output shape will crash or produce meaningless results.

**Correction**: Use the correct `ptychography` operator which produces the physically valid output shape (16, 32, 32).

### Mismatch Example 2: Round-Trip Fidelity

| Metric | Correct | Widefield Fallback |
|--------|---------|-------------------|
| Output shape | (64, 64) | (64, 64) |
| Shapes match | Yes | — |
| Round-trip RMSE | 1.024245 | — |

**Failure**: Correct round-trip RMSE = 1.024245, fallback round-trip RMSE = 0.979956. The widefield round-trip loses structure-specific information that the correct operator preserves.

**Correction**: Use the paired `ptychography` forward/adjoint for proper forward modeling and reconstruction.

### Mismatch Example 3: Linearity Mismatch — Non-linear vs Linear Model

| Metric | Correct | Widefield Fallback |
|--------|---------|-------------------|
| Output shape | (16, 32, 32) | (64, 64) |
| Shapes match | **No** | — |

**Failure**: The correct operator is non-linear: A(a*x1 + b*x2) != a*A(x1) + b*A(x2). Linearity error = 33.766057. The widefield fallback is always linear (Gaussian blur), missing the non-linear physics entirely.

**Correction**: Use `ptychography` which correctly models the non-linear forward process: I_j = |FFT(P(r) * O(r - r_j))|^2.

---

## 14. Digital Holographic Microscopy (`holography`)

**Physics**: Digital holographic microscopy records the interference pattern between the object wave and a reference wave. The complex-valued wave propagation (Fresnel/angular spectrum) produces an interference hologram. The widefield fallback loses all phase information by producing only real-valued output.

**Forward equation**: `I = |U_obj * exp(i*phi) + U_ref|^2`

**Correct operator**: `dedicated` | **Input shape**: (64, 64) | **Output shape**: (64, 64) | **Linear**: No

### Mismatch Example 1: Forward Pass Comparison

| Metric | Correct | Widefield Fallback |
|--------|---------|-------------------|
| Output shape | (64, 64) | (64, 64) |
| Shapes match | Yes | — |
| Complex output | No | No |
| RMSE | 2.842948 | — |
| Correlation | -0.0091 | — |
| Max abs diff | 20.621442 | — |
| Relative RMSE | 1.0066 | — |

**Failure**: While output shapes match, the pixel values are fundamentally different. RMSE = 2.842948, correlation = -0.0091. The widefield blur applies a generic Gaussian PSF that does not capture the physics of Digital Holographic Microscopy.

**Correction**: Use the correct `holography` operator which implements the physical forward model: I = |U_obj * exp(i*phi) + U_ref|^2.

### Mismatch Example 2: Round-Trip Fidelity

| Metric | Correct | Widefield Fallback |
|--------|---------|-------------------|
| Output shape | (64, 64) | (64, 64) |
| Shapes match | Yes | — |
| Round-trip RMSE | 1.102913 | — |

**Failure**: Correct round-trip RMSE = 1.102913, fallback round-trip RMSE = 0.979956. The widefield round-trip loses structure-specific information that the correct operator preserves.

**Correction**: Use the paired `holography` forward/adjoint for proper forward modeling and reconstruction.

---

## 15. Coherent Diffractive Imaging (CDI) (`phase_retrieval`)

**Physics**: Phase retrieval / CDI measures only the intensity (squared modulus) of the Fourier transform of the sample transmittance. This is an inherently non-linear forward model (loss of phase). The widefield linear convolution cannot represent Fourier intensity measurement.

**Forward equation**: `y = |F{x}|^2`

**Correct operator**: `dedicated` | **Input shape**: (64, 64) | **Output shape**: (64, 64) | **Linear**: No

### Mismatch Example 1: Forward Pass Comparison

| Metric | Correct | Widefield Fallback |
|--------|---------|-------------------|
| Output shape | (64, 64) | (64, 64) |
| Shapes match | Yes | — |
| Complex output | No | No |
| RMSE | 5759.289915 | — |
| Correlation | -0.0119 | — |
| Max abs diff | 30303.107728 | — |
| Relative RMSE | 1.0000 | — |

**Failure**: While output shapes match, the pixel values are fundamentally different. RMSE = 5759.289915, correlation = -0.0119. The widefield blur applies a generic Gaussian PSF that does not capture the physics of Coherent Diffractive Imaging (CDI).

**Correction**: Use the correct `phase_retrieval` operator which implements the physical forward model: y = |F{x}|^2.

### Mismatch Example 2: Round-Trip Fidelity

| Metric | Correct | Widefield Fallback |
|--------|---------|-------------------|
| Output shape | (64, 64) | (64, 64) |
| Shapes match | Yes | — |
| Round-trip RMSE | 1.405553 | — |

**Failure**: Correct round-trip RMSE = 1.405553, fallback round-trip RMSE = 0.979956. The widefield round-trip loses structure-specific information that the correct operator preserves.

**Correction**: Use the paired `phase_retrieval` forward/adjoint for proper forward modeling and reconstruction.

### Mismatch Example 3: Linearity Mismatch — Non-linear vs Linear Model

| Metric | Correct | Widefield Fallback |
|--------|---------|-------------------|
| Output shape | (64, 64) | (64, 64) |
| Shapes match | Yes | — |

**Failure**: The correct operator is non-linear: A(a*x1 + b*x2) != a*A(x1) + b*A(x2). Linearity error = 688.083417. The widefield fallback is always linear (Gaussian blur), missing the non-linear physics entirely.

**Correction**: Use `phase_retrieval` which correctly models the non-linear forward process: y = |F{x}|^2.

---

## 16. Fourier Ptychographic Microscopy (`fpm`)

**Physics**: FPM illuminates the sample from multiple angles to synthetically extend the microscope's numerical aperture. Each angle captures |F^{-1}{P(k-k_j) * O(k)}|^2, a non-linear intensity measurement. The widefield blur captures none of the angular diversity or Fourier synthesis.

**Forward equation**: `y_j = |F^{-1}{P(k - k_j) * O(k)}|^2`

**Correct operator**: `dedicated` | **Input shape**: (64, 64) | **Output shape**: (25, 16, 16) | **Linear**: No

### Mismatch Example 1: Forward Pass Comparison

| Metric | Correct | Widefield Fallback |
|--------|---------|-------------------|
| Output shape | (25, 16, 16) | (64, 64) |
| Shapes match | **No** | — |
| Complex output | No | No |

**Failure**: The correct forward model produces output of shape (25, 16, 16), but the widefield fallback produces shape (64, 64). Any reconstruction algorithm expecting the correct output shape will crash or produce meaningless results.

**Correction**: Use the correct `fpm` operator which produces the physically valid output shape (25, 16, 16).

### Mismatch Example 2: Round-Trip Fidelity

| Metric | Correct | Widefield Fallback |
|--------|---------|-------------------|
| Output shape | (64, 64) | (64, 64) |
| Shapes match | Yes | — |
| Round-trip RMSE | 1.000201 | — |

**Failure**: Correct round-trip RMSE = 1.000201, fallback round-trip RMSE = 0.979956. The widefield round-trip loses structure-specific information that the correct operator preserves.

**Correction**: Use the paired `fpm` forward/adjoint for proper forward modeling and reconstruction.

### Mismatch Example 3: Linearity Mismatch — Non-linear vs Linear Model

| Metric | Correct | Widefield Fallback |
|--------|---------|-------------------|
| Output shape | (25, 16, 16) | (64, 64) |
| Shapes match | **No** | — |

**Failure**: The correct operator is non-linear: A(a*x1 + b*x2) != a*A(x1) + b*A(x2). Linearity error = 2.244174. The widefield fallback is always linear (Gaussian blur), missing the non-linear physics entirely.

**Correction**: Use `fpm` which correctly models the non-linear forward process: y_j = |F^{-1}{P(k - k_j) * O(k)}|^2.

---

## 17. Neural Radiance Fields (NeRF) (`nerf`)

**Physics**: NeRF represents a scene as a continuous volumetric function mapping 3D coordinates to density and color. Rendering integrates along camera rays through the volume. The correct operator takes a 3D volume and produces multiple 2D views — the widefield 2D blur is dimensionally incompatible.

**Forward equation**: `C(r) = integral T(t) * sigma(r(t)) * c(r(t),d) dt`

**Correct operator**: `dedicated` | **Input shape**: (64, 64, 32) | **Output shape**: (10, 64, 64) | **Linear**: No

### Mismatch Example 1: Forward Pass Comparison

| Metric | Correct | Widefield Fallback |
|--------|---------|-------------------|
| Output shape | (10, 64, 64) | (64, 64) |
| Shapes match | **No** | — |
| Complex output | No | No |

**Failure**: The correct forward model produces output of shape (10, 64, 64), but the widefield fallback produces shape (64, 64). Any reconstruction algorithm expecting the correct output shape will crash or produce meaningless results.

**Correction**: Use the correct `nerf` operator which produces the physically valid output shape (10, 64, 64).

### Mismatch Example 2: Round-Trip Fidelity

| Metric | Correct | Widefield Fallback |
|--------|---------|-------------------|
| Output shape | (64, 64, 32) | (64, 64) |
| Shapes match | **No** | — |
| Round-trip RMSE | 0.992179 | — |

**Failure**: Correct round-trip RMSE = 0.992179, fallback round-trip RMSE = 0.976433. The widefield round-trip loses structure-specific information that the correct operator preserves.

**Correction**: Use the paired `nerf` forward/adjoint for proper forward modeling and reconstruction.

---

## 18. 3D Gaussian Splatting (`gaussian_splatting`)

**Physics**: 3D Gaussian Splatting renders scenes by projecting anisotropic 3D Gaussians onto the image plane with alpha compositing. Like NeRF, it takes a 3D volume and renders multi-view 2D images. The non-linear splatting+compositing is fundamentally different from a 2D Gaussian blur.

**Forward equation**: `C(p) = sum_i alpha_i * c_i * prod_{j<i}(1 - alpha_j)`

**Correct operator**: `dedicated` | **Input shape**: (64, 64, 32) | **Output shape**: (10, 64, 64) | **Linear**: No

### Mismatch Example 1: Forward Pass Comparison

| Metric | Correct | Widefield Fallback |
|--------|---------|-------------------|
| Output shape | (10, 64, 64) | (64, 64) |
| Shapes match | **No** | — |
| Complex output | No | No |

**Failure**: The correct forward model produces output of shape (10, 64, 64), but the widefield fallback produces shape (64, 64). Any reconstruction algorithm expecting the correct output shape will crash or produce meaningless results.

**Correction**: Use the correct `gaussian_splatting` operator which produces the physically valid output shape (10, 64, 64).

### Mismatch Example 2: Round-Trip Fidelity

| Metric | Correct | Widefield Fallback |
|--------|---------|-------------------|
| Output shape | (64, 64, 32) | (64, 64) |
| Shapes match | **No** | — |
| Round-trip RMSE | 1.003233 | — |

**Failure**: Correct round-trip RMSE = 1.003233, fallback round-trip RMSE = 0.976433. The widefield round-trip loses structure-specific information that the correct operator preserves.

**Correction**: Use the paired `gaussian_splatting` forward/adjoint for proper forward modeling and reconstruction.

---

## 19. Lensless (Diffuser Camera) Imaging (`lensless`)

**Physics**: Lensless imaging replaces the lens with a coded optical element (diffuser or mask), producing a heavily convolved measurement. The PSF is typically much larger (sigma~10) and spatially varying compared to widefield (sigma=2.0), resulting in fundamentally different blur characteristics.

**Forward equation**: `y = PSF_diffuser ** x + n`

**Correct operator**: `dedicated` | **Input shape**: (64, 64) | **Output shape**: (64, 64) | **Linear**: Yes

### Mismatch Example 1: Forward Pass Comparison

| Metric | Correct | Widefield Fallback |
|--------|---------|-------------------|
| Output shape | (64, 64) | (64, 64) |
| Shapes match | Yes | — |
| Complex output | No | No |
| RMSE | 0.959978 | — |
| Correlation | 0.3015 | — |
| Max abs diff | 3.232664 | — |
| Relative RMSE | 0.9649 | — |

**Failure**: While output shapes match, the pixel values are fundamentally different. RMSE = 0.959978, correlation = 0.3015. The widefield blur applies a generic Gaussian PSF that does not capture the physics of Lensless (Diffuser Camera) Imaging.

**Correction**: Use the correct `lensless` operator which implements the physical forward model: y = PSF_diffuser ** x + n.

### Mismatch Example 2: Round-Trip Fidelity

| Metric | Correct | Widefield Fallback |
|--------|---------|-------------------|
| Output shape | (64, 64) | (64, 64) |
| Shapes match | Yes | — |
| Round-trip RMSE | 0.005742 | — |

**Failure**: Correct round-trip RMSE = 0.005742, fallback round-trip RMSE = 0.979956. The widefield round-trip loses structure-specific information that the correct operator preserves.

**Correction**: Use the paired `lensless` forward/adjoint for proper forward modeling and reconstruction.

---

## 20. Panorama Multi-Focus Fusion (`panorama`)

**Physics**: Panoramic imaging captures multiple focal planes and stitches them into a single all-in-focus composite. The forward model includes geometric warping (parallax shifts) and depth-dependent blur — neither of which is captured by the simple Gaussian convolution fallback.

**Forward equation**: `y_k = PSF(d_k) ** x + n, k=1..N_focus`

**Correct operator**: `graph` | **Input shape**: (64, 64) | **Output shape**: (64, 64) | **Linear**: Yes

### Mismatch Example 1: Forward Pass Comparison

| Metric | Correct | Widefield Fallback |
|--------|---------|-------------------|
| Output shape | (64, 64) | (64, 64) |
| Shapes match | Yes | — |
| Complex output | No | No |
| RMSE | 0.259641 | — |
| Correlation | 0.4530 | — |
| Max abs diff | 1.084103 | — |
| Relative RMSE | 0.8928 | — |

**Failure**: While output shapes match, the pixel values are fundamentally different. RMSE = 0.259641, correlation = 0.4530. The widefield blur applies a generic Gaussian PSF that does not capture the physics of Panorama Multi-Focus Fusion.

**Correction**: Use the correct `panorama_graph_v1` operator which implements the physical forward model: y_k = PSF(d_k) ** x + n, k=1..N_focus.

### Mismatch Example 2: Round-Trip Fidelity

| Metric | Correct | Widefield Fallback |
|--------|---------|-------------------|
| Output shape | (64, 64) | (64, 64) |
| Shapes match | Yes | — |
| Round-trip RMSE | 0.932407 | — |

**Failure**: Correct round-trip RMSE = 0.932407, fallback round-trip RMSE = 0.979956. The widefield round-trip loses structure-specific information that the correct operator preserves.

**Correction**: Use the paired `panorama_graph_v1` forward/adjoint for proper forward modeling and reconstruction.

---

## 21. Light Field Imaging (`light_field`)

**Physics**: Light field cameras use a microlens array to capture both spatial and angular information of the light field. The forward model involves disparity-dependent shifts and microlens integration that produce fundamentally different measurements from simple blur.

**Forward equation**: `y(x,y) = sum_{u,v} L(x,y,u,v) * MLA(x,y,u,v)`

**Correct operator**: `dedicated` | **Input shape**: (64, 64, 5, 5) | **Output shape**: (64, 64) | **Linear**: Yes

### Mismatch Example 1: Forward Pass Comparison

| Metric | Correct | Widefield Fallback |
|--------|---------|-------------------|
| Output shape | (64, 64) | (64, 5) |
| Shapes match | **No** | — |
| Complex output | No | No |

**Failure**: The correct forward model produces output of shape (64, 64), but the widefield fallback produces shape (64, 5). Any reconstruction algorithm expecting the correct output shape will crash or produce meaningless results.

**Correction**: Use the correct `light_field` operator which produces the physically valid output shape (64, 64).

### Mismatch Example 2: Round-Trip Fidelity

| Metric | Correct | Widefield Fallback |
|--------|---------|-------------------|
| Output shape | (64, 64, 5, 5) | (64, 5) |
| Shapes match | **No** | — |
| Round-trip RMSE | 1.002152 | — |

**Failure**: Correct round-trip RMSE = 1.002152, fallback round-trip RMSE = 0.943213. The widefield round-trip loses structure-specific information that the correct operator preserves.

**Correction**: Use the paired `light_field` forward/adjoint for proper forward modeling and reconstruction.

---

## 22. Diffuse Optical Tomography (`dot`)

**Physics**: DOT reconstructs internal tissue optical properties from surface boundary measurements. Multiple source-detector pairs on the surface measure diffuse photon propagation through tissue. The output is a 1D measurement vector — not a 2D blurred image.

**Forward equation**: `y = J(mu_a, mu_s') * x`

**Correct operator**: `dedicated` | **Input shape**: (64, 64, 64) | **Output shape**: (64) | **Linear**: Yes

### Mismatch Example 1: Forward Pass Comparison

| Metric | Correct | Widefield Fallback |
|--------|---------|-------------------|
| Output shape | (64) | (64, 64) |
| Shapes match | **No** | — |
| Complex output | No | No |

**Failure**: The correct forward model produces output of shape (64,), but the widefield fallback produces shape (64, 64). Any reconstruction algorithm expecting the correct output shape will crash or produce meaningless results.

**Correction**: Use the correct `dot` operator which produces the physically valid output shape (64,).

### Mismatch Example 2: Round-Trip Fidelity

| Metric | Correct | Widefield Fallback |
|--------|---------|-------------------|
| Output shape | (64, 64, 64) | (64, 64) |
| Shapes match | **No** | — |
| Round-trip RMSE | 1.001254 | — |

**Failure**: Correct round-trip RMSE = 1.001254, fallback round-trip RMSE = 0.979956. The widefield round-trip loses structure-specific information that the correct operator preserves.

**Correction**: Use the paired `dot` forward/adjoint for proper forward modeling and reconstruction.

---

## 23. Photoacoustic Imaging (`photoacoustic`)

**Physics**: Photoacoustic imaging converts pulsed laser illumination into acoustic waves via thermoelastic expansion. Ultrasonic transducers detect the acoustic signals. The forward model involves a circular Radon transform producing time-series data for each transducer, not a spatially blurred 2D image.

**Forward equation**: `y = R * p0, p0 = Gamma * mu_a * Phi`

**Correct operator**: `dedicated` | **Input shape**: (64, 64) | **Output shape**: (32, 91) | **Linear**: Yes

### Mismatch Example 1: Forward Pass Comparison

| Metric | Correct | Widefield Fallback |
|--------|---------|-------------------|
| Output shape | (32, 91) | (64, 64) |
| Shapes match | **No** | — |
| Complex output | No | No |

**Failure**: The correct forward model produces output of shape (32, 91), but the widefield fallback produces shape (64, 64). Any reconstruction algorithm expecting the correct output shape will crash or produce meaningless results.

**Correction**: Use the correct `photoacoustic` operator which produces the physically valid output shape (32, 91).

### Mismatch Example 2: Round-Trip Fidelity

| Metric | Correct | Widefield Fallback |
|--------|---------|-------------------|
| Output shape | (64, 64) | (64, 64) |
| Shapes match | Yes | — |
| Round-trip RMSE | 75.504334 | — |

**Failure**: Correct round-trip RMSE = 75.504334, fallback round-trip RMSE = 0.979956. The widefield round-trip loses structure-specific information that the correct operator preserves.

**Correction**: Use the paired `photoacoustic` forward/adjoint for proper forward modeling and reconstruction.

---

## 24. Optical Coherence Tomography (`oct`)

**Physics**: OCT uses low-coherence interferometry to obtain depth-resolved images. The forward model involves spectral modulation and Fourier transform relations between sample reflectivity and detected spectral interferogram. The widefield blur cannot represent the spectral encoding.

**Forward equation**: `y(k) = |E_r + E_s(k)|^2`

**Correct operator**: `dedicated` | **Input shape**: (64, 64) | **Output shape**: (64, 128) | **Linear**: No

### Mismatch Example 1: Forward Pass Comparison

| Metric | Correct | Widefield Fallback |
|--------|---------|-------------------|
| Output shape | (64, 128) | (64, 64) |
| Shapes match | **No** | — |
| Complex output | No | No |

**Failure**: The correct forward model produces output of shape (64, 128), but the widefield fallback produces shape (64, 64). Any reconstruction algorithm expecting the correct output shape will crash or produce meaningless results.

**Correction**: Use the correct `oct` operator which produces the physically valid output shape (64, 128).

### Mismatch Example 2: Round-Trip Fidelity

| Metric | Correct | Widefield Fallback |
|--------|---------|-------------------|
| Output shape | (64, 64) | (64, 64) |
| Shapes match | Yes | — |
| Round-trip RMSE | 259.746293 | — |

**Failure**: Correct round-trip RMSE = 259.746293, fallback round-trip RMSE = 0.979956. The widefield round-trip loses structure-specific information that the correct operator preserves.

**Correction**: Use the paired `oct` forward/adjoint for proper forward modeling and reconstruction.

---

## 25. Fluorescence Lifetime Imaging (`flim`)

**Physics**: FLIM measures the fluorescence decay kinetics at each pixel, producing a 3D data cube (spatial x temporal). The forward model convolves the multi-exponential decay with the instrument response function. The widefield blur ignores temporal dynamics entirely.

**Forward equation**: `y(t) = IRF(t) * [sum_i a_i * exp(-t/tau_i)]`

**Correct operator**: `dedicated` | **Input shape**: (64, 64, 2) | **Output shape**: (64, 64, 64) | **Linear**: No

### Mismatch Example 1: Forward Pass Comparison

| Metric | Correct | Widefield Fallback |
|--------|---------|-------------------|
| Output shape | (64, 64, 64) | (64, 64) |
| Shapes match | **No** | — |
| Complex output | No | No |

**Failure**: The correct forward model produces output of shape (64, 64, 64), but the widefield fallback produces shape (64, 64). Any reconstruction algorithm expecting the correct output shape will crash or produce meaningless results.

**Correction**: Use the correct `flim` operator which produces the physically valid output shape (64, 64, 64).

### Mismatch Example 2: Round-Trip Fidelity

| Metric | Correct | Widefield Fallback |
|--------|---------|-------------------|
| Output shape | (64, 64, 2) | (64, 64) |
| Shapes match | **No** | — |
| Round-trip RMSE | 1.478367 | — |

**Failure**: Correct round-trip RMSE = 1.478367, fallback round-trip RMSE = 0.971454. The widefield round-trip loses structure-specific information that the correct operator preserves.

**Correction**: Use the paired `flim` forward/adjoint for proper forward modeling and reconstruction.

### Mismatch Example 3: Linearity Mismatch — Non-linear vs Linear Model

| Metric | Correct | Widefield Fallback |
|--------|---------|-------------------|
| Output shape | (64, 64, 64) | (64, 64) |
| Shapes match | **No** | — |

**Failure**: The correct operator is non-linear: A(a*x1 + b*x2) != a*A(x1) + b*A(x2). Linearity error = 0.014557. The widefield fallback is always linear (Gaussian blur), missing the non-linear physics entirely.

**Correction**: Use `flim` which correctly models the non-linear forward process: y(t) = IRF(t) * [sum_i a_i * exp(-t/tau_i)].

---

## 26. Integral Photography (Plenoptic) (`integral`)

**Physics**: Integral photography (plenoptic imaging) uses a microlens array to capture multi-view sub-images at different depths. The forward model produces a set of depth-dependent views, which the widefield fallback collapses into a single blurred 2D image.

**Forward equation**: `I(x,y) = integral L(x,y,u,v) * T(u,v) dudv`

**Correct operator**: `dedicated` | **Input shape**: (64, 64, 8) | **Output shape**: (64, 64) | **Linear**: Yes

### Mismatch Example 1: Forward Pass Comparison

| Metric | Correct | Widefield Fallback |
|--------|---------|-------------------|
| Output shape | (64, 64) | (64, 64) |
| Shapes match | Yes | — |
| Complex output | No | No |
| RMSE | 0.142011 | — |
| Correlation | 0.4016 | — |
| Max abs diff | 0.550122 | — |
| Relative RMSE | 1.6015 | — |

**Failure**: While output shapes match, the pixel values are fundamentally different. RMSE = 0.142011, correlation = 0.4016. The widefield blur applies a generic Gaussian PSF that does not capture the physics of Integral Photography (Plenoptic).

**Correction**: Use the correct `integral` operator which implements the physical forward model: I(x,y) = integral L(x,y,u,v) * T(u,v) dudv.

### Mismatch Example 2: Round-Trip Fidelity

| Metric | Correct | Widefield Fallback |
|--------|---------|-------------------|
| Output shape | (64, 64, 8) | (64, 64) |
| Shapes match | **No** | — |
| Round-trip RMSE | 1.003784 | — |

**Failure**: Correct round-trip RMSE = 1.003784, fallback round-trip RMSE = 0.970420. The widefield round-trip loses structure-specific information that the correct operator preserves.

**Correction**: Use the paired `integral` forward/adjoint for proper forward modeling and reconstruction.

---

## 27. X-ray Radiography (`xray_radiography`)

**Physics**: X-ray radiography measures the transmission of X-rays through tissue following Beer-Lambert exponential attenuation. The non-linear exponential model (I = I_0 * exp(-mu*x)) produces contrast fundamentally different from linear Gaussian convolution.

**Forward equation**: `y = eta * I_0 * exp(-mu * x) + scatter + n`

**Correct operator**: `dedicated` | **Input shape**: (64, 64) | **Output shape**: (64, 64) | **Linear**: No

### Mismatch Example 1: Forward Pass Comparison

| Metric | Correct | Widefield Fallback |
|--------|---------|-------------------|
| Output shape | (64, 64) | (64, 64) |
| Shapes match | Yes | — |
| Complex output | No | No |
| RMSE | 0.598136 | — |
| Correlation | 0.4400 | — |
| Max abs diff | 2.092110 | — |
| Relative RMSE | 0.9210 | — |

**Failure**: While output shapes match, the pixel values are fundamentally different. RMSE = 0.598136, correlation = 0.4400. The widefield blur applies a generic Gaussian PSF that does not capture the physics of X-ray Radiography.

**Correction**: Use the correct `xray_radiography` operator which implements the physical forward model: y = eta * I_0 * exp(-mu * x) + scatter + n.

### Mismatch Example 2: Round-Trip Fidelity

| Metric | Correct | Widefield Fallback |
|--------|---------|-------------------|
| Output shape | (64, 64) | (64, 64) |
| Shapes match | Yes | — |
| Round-trip RMSE | 0.619072 | — |

**Failure**: Correct round-trip RMSE = 0.619072, fallback round-trip RMSE = 0.979956. The widefield round-trip loses structure-specific information that the correct operator preserves.

**Correction**: Use the paired `xray_radiography` forward/adjoint for proper forward modeling and reconstruction.

---

## 28. Ultrasound Imaging (`ultrasound`)

**Physics**: Ultrasound imaging sends acoustic pulses into tissue and records echo signals at an array of transducer elements. The output is RF channel data of shape (n_elements, n_samples) — a fundamentally different domain from a spatially blurred 2D image.

**Forward equation**: `y = DAS(H * x) + n`

**Correct operator**: `dedicated` | **Input shape**: (64, 64) | **Output shape**: (32, 128) | **Linear**: Yes

### Mismatch Example 1: Forward Pass Comparison

| Metric | Correct | Widefield Fallback |
|--------|---------|-------------------|
| Output shape | (32, 128) | (64, 64) |
| Shapes match | **No** | — |
| Complex output | No | No |

**Failure**: The correct forward model produces output of shape (32, 128), but the widefield fallback produces shape (64, 64). Any reconstruction algorithm expecting the correct output shape will crash or produce meaningless results.

**Correction**: Use the correct `ultrasound` operator which produces the physically valid output shape (32, 128).

### Mismatch Example 2: Round-Trip Fidelity

| Metric | Correct | Widefield Fallback |
|--------|---------|-------------------|
| Output shape | (64, 64) | (64, 64) |
| Shapes match | Yes | — |
| Round-trip RMSE | 976.269374 | — |

**Failure**: Correct round-trip RMSE = 976.269374, fallback round-trip RMSE = 0.979956. The widefield round-trip loses structure-specific information that the correct operator preserves.

**Correction**: Use the paired `ultrasound` forward/adjoint for proper forward modeling and reconstruction.

---

## 29. Positron Emission Tomography (`pet`)

**Physics**: PET detects annihilation photon pairs to reconstruct radiotracer distribution. The forward model projects the activity distribution at multiple angles producing a sinogram. The widefield blur has no concept of angular projections.

**Forward equation**: `y = Poisson(A * x + scatter)`

**Correct operator**: `dedicated` | **Input shape**: (64, 64) | **Output shape**: (32, 64) | **Linear**: Yes

### Mismatch Example 1: Forward Pass Comparison

| Metric | Correct | Widefield Fallback |
|--------|---------|-------------------|
| Output shape | (32, 64) | (64, 64) |
| Shapes match | **No** | — |
| Complex output | No | No |

**Failure**: The correct forward model produces output of shape (32, 64), but the widefield fallback produces shape (64, 64). Any reconstruction algorithm expecting the correct output shape will crash or produce meaningless results.

**Correction**: Use the correct `pet` operator which produces the physically valid output shape (32, 64).

### Mismatch Example 2: Round-Trip Fidelity

| Metric | Correct | Widefield Fallback |
|--------|---------|-------------------|
| Output shape | (64, 64) | (64, 64) |
| Shapes match | Yes | — |
| Round-trip RMSE | 57.071892 | — |

**Failure**: Correct round-trip RMSE = 57.071892, fallback round-trip RMSE = 0.979956. The widefield round-trip loses structure-specific information that the correct operator preserves.

**Correction**: Use the paired `pet` forward/adjoint for proper forward modeling and reconstruction.

---

## 30. Single Photon Emission CT (`spect`)

**Physics**: SPECT images gamma-ray emitting radiotracers using collimated detectors that rotate around the patient. The collimator creates a depth-dependent blur in the projection, producing sinogram data incompatible with simple 2D Gaussian convolution.

**Forward equation**: `y = Poisson(C * A * x)`

**Correct operator**: `dedicated` | **Input shape**: (64, 64) | **Output shape**: (32, 64) | **Linear**: Yes

### Mismatch Example 1: Forward Pass Comparison

| Metric | Correct | Widefield Fallback |
|--------|---------|-------------------|
| Output shape | (32, 64) | (64, 64) |
| Shapes match | **No** | — |
| Complex output | No | No |

**Failure**: The correct forward model produces output of shape (32, 64), but the widefield fallback produces shape (64, 64). Any reconstruction algorithm expecting the correct output shape will crash or produce meaningless results.

**Correction**: Use the correct `spect` operator which produces the physically valid output shape (32, 64).

### Mismatch Example 2: Round-Trip Fidelity

| Metric | Correct | Widefield Fallback |
|--------|---------|-------------------|
| Output shape | (64, 64) | (64, 64) |
| Shapes match | Yes | — |
| Round-trip RMSE | 45.917346 | — |

**Failure**: Correct round-trip RMSE = 45.917346, fallback round-trip RMSE = 0.979956. The widefield round-trip loses structure-specific information that the correct operator preserves.

**Correction**: Use the paired `spect` forward/adjoint for proper forward modeling and reconstruction.

---

## 31. Scanning Electron Microscopy (`sem`)

**Physics**: SEM forms images by scanning a focused electron beam across the sample and detecting secondary or backscattered electrons. The signal depends on material properties (yield coefficient) and beam-sample interaction, not optical PSF convolution.

**Forward equation**: `y = G * eta(E_0) * I_b * x + n`

**Correct operator**: `dedicated` | **Input shape**: (64, 64) | **Output shape**: (64, 64) | **Linear**: Yes

### Mismatch Example 1: Forward Pass Comparison

| Metric | Correct | Widefield Fallback |
|--------|---------|-------------------|
| Output shape | (64, 64) | (64, 64) |
| Shapes match | Yes | — |
| Complex output | No | No |
| RMSE | 0.100950 | — |
| Correlation | 0.8103 | — |
| Max abs diff | 0.392398 | — |
| Relative RMSE | 1.3092 | — |

**Failure**: While output shapes match, the pixel values are fundamentally different. RMSE = 0.100950, correlation = 0.8103. The widefield blur applies a generic Gaussian PSF that does not capture the physics of Scanning Electron Microscopy.

**Correction**: Use the correct `sem` operator which implements the physical forward model: y = G * eta(E_0) * I_b * x + n.

### Mismatch Example 2: Round-Trip Fidelity

| Metric | Correct | Widefield Fallback |
|--------|---------|-------------------|
| Output shape | (64, 64) | (64, 64) |
| Shapes match | Yes | — |
| Round-trip RMSE | 0.991757 | — |

**Failure**: Correct round-trip RMSE = 0.991757, fallback round-trip RMSE = 0.979956. The widefield round-trip loses structure-specific information that the correct operator preserves.

**Correction**: Use the paired `sem` forward/adjoint for proper forward modeling and reconstruction.

---

## 32. Transmission Electron Microscopy (`tem`)

**Physics**: TEM transmits an electron beam through a thin specimen. Image contrast arises from the contrast transfer function (CTF) applied in Fourier space, which produces complex-valued output with oscillating phase contrast. The widefield real-valued blur cannot represent CTF oscillations.

**Forward equation**: `y = |IFFT(CTF * FFT(exp(i*sigma*V*x)))|^2 + n`

**Correct operator**: `dedicated` | **Input shape**: (64, 64) | **Output shape**: (64, 64) | **Linear**: No

### Mismatch Example 1: Forward Pass Comparison

| Metric | Correct | Widefield Fallback |
|--------|---------|-------------------|
| Output shape | (64, 64) | (64, 64) |
| Shapes match | Yes | — |
| Complex output | No | No |
| RMSE | 0.173497 | — |
| Correlation | -0.0220 | — |
| Max abs diff | 0.784027 | — |
| Relative RMSE | 2.1959 | — |

**Failure**: While output shapes match, the pixel values are fundamentally different. RMSE = 0.173497, correlation = -0.0220. The widefield blur applies a generic Gaussian PSF that does not capture the physics of Transmission Electron Microscopy.

**Correction**: Use the correct `tem` operator which implements the physical forward model: y = |IFFT(CTF * FFT(exp(i*sigma*V*x)))|^2 + n.

### Mismatch Example 2: Round-Trip Fidelity

| Metric | Correct | Widefield Fallback |
|--------|---------|-------------------|
| Output shape | (64, 64) | (64, 64) |
| Shapes match | Yes | — |
| Round-trip RMSE | 0.991399 | — |

**Failure**: Correct round-trip RMSE = 0.991399, fallback round-trip RMSE = 0.979956. The widefield round-trip loses structure-specific information that the correct operator preserves.

**Correction**: Use the paired `tem` forward/adjoint for proper forward modeling and reconstruction.

---

## 33. Electron Tomography (`electron_tomography`)

**Physics**: Electron tomography acquires a tilt series of TEM projections at multiple angles through a 3D specimen volume. The forward model is a 3D-to-2D projection operator — the widefield 2D blur has no concept of tilt-angle projections through a volume.

**Forward equation**: `y_i = R(angle_i) * volume + n_i`

**Correct operator**: `dedicated` | **Input shape**: (32, 64, 64) | **Output shape**: (16, 64, 64) | **Linear**: Yes

### Mismatch Example 1: Forward Pass Comparison

| Metric | Correct | Widefield Fallback |
|--------|---------|-------------------|
| Output shape | (16, 64, 64) | (64, 64) |
| Shapes match | **No** | — |
| Complex output | No | No |

**Failure**: The correct forward model produces output of shape (16, 64, 64), but the widefield fallback produces shape (64, 64). Any reconstruction algorithm expecting the correct output shape will crash or produce meaningless results.

**Correction**: Use the correct `electron_tomography` operator which produces the physically valid output shape (16, 64, 64).

### Mismatch Example 2: Round-Trip Fidelity

| Metric | Correct | Widefield Fallback |
|--------|---------|-------------------|
| Output shape | (32, 64, 64) | (64, 64) |
| Shapes match | **No** | — |
| Round-trip RMSE | 22.437046 | — |

**Failure**: Correct round-trip RMSE = 22.437046, fallback round-trip RMSE = 0.979956. The widefield round-trip loses structure-specific information that the correct operator preserves.

**Correction**: Use the paired `electron_tomography` forward/adjoint for proper forward modeling and reconstruction.

---

## 34. Scanning TEM (STEM) (`stem`)

**Physics**: STEM scans a focused electron probe across the sample, detecting transmitted electrons with an annular detector. Image contrast depends on probe convolution and atomic-number-dependent scattering (Z-contrast), not simple Gaussian blur.

**Forward equation**: `y = G * eta * (probe ** x) + n`

**Correct operator**: `graph` | **Input shape**: (64, 64) | **Output shape**: (64, 64) | **Linear**: Yes

### Mismatch Example 1: Forward Pass Comparison

| Metric | Correct | Widefield Fallback |
|--------|---------|-------------------|
| Output shape | (64, 64) | (64, 64) |
| Shapes match | Yes | — |
| Complex output | No | No |
| RMSE | 0.159546 | — |
| Correlation | -0.0195 | — |
| Max abs diff | 0.651958 | — |
| Relative RMSE | 5.2351 | — |

**Failure**: While output shapes match, the pixel values are fundamentally different. RMSE = 0.159546, correlation = -0.0195. The widefield blur applies a generic Gaussian PSF that does not capture the physics of Scanning TEM (STEM).

**Correction**: Use the correct `stem_graph_v2` operator which implements the physical forward model: y = G * eta * (probe ** x) + n.

### Mismatch Example 2: Round-Trip Fidelity

| Metric | Correct | Widefield Fallback |
|--------|---------|-------------------|
| Output shape | (64, 64) | (64, 64) |
| Shapes match | Yes | — |

**Failure**: The correct operator's adjoint is not available (non-linear model). The widefield round-trip (blur->blur) simply double-blurs the image, which does not represent the non-linear inverse problem.

**Correction**: For non-linear modalities like Scanning TEM (STEM), iterative algorithms (gradient descent, ADMM) must use the correct forward model.

---

## 35. Fluoroscopy (`fluoroscopy`)

**Physics**: Fluoroscopy provides real-time X-ray imaging via Beer-Lambert attenuation with temporal integration of multiple low-dose frames. The exponential attenuation and temporal averaging produce contrast unlike simple Gaussian convolution.

**Forward equation**: `y = (1/N) * sum_t eta * I_0 * exp(-mu * x_t) + n`

**Correct operator**: `graph` | **Input shape**: (64, 64) | **Output shape**: (64, 64) | **Linear**: No

### Mismatch Example 1: Forward Pass Comparison

| Metric | Correct | Widefield Fallback |
|--------|---------|-------------------|
| Output shape | (64, 64) | (64, 64) |
| Shapes match | Yes | — |
| Complex output | No | No |
| RMSE | 9388.902675 | — |
| Correlation | -0.2433 | — |
| Max abs diff | 134444.005390 | — |
| Relative RMSE | 1.0000 | — |

**Failure**: While output shapes match, the pixel values are fundamentally different. RMSE = 9388.902675, correlation = -0.2433. The widefield blur applies a generic Gaussian PSF that does not capture the physics of Fluoroscopy.

**Correction**: Use the correct `fluoroscopy_graph_v2` operator which implements the physical forward model: y = (1/N) * sum_t eta * I_0 * exp(-mu * x_t) + n.

### Mismatch Example 2: Round-Trip Fidelity

| Metric | Correct | Widefield Fallback |
|--------|---------|-------------------|
| Output shape | (64, 64) | (64, 64) |
| Shapes match | Yes | — |

**Failure**: The correct operator's adjoint is not available (non-linear model). The widefield round-trip (blur->blur) simply double-blurs the image, which does not represent the non-linear inverse problem.

**Correction**: For non-linear modalities like Fluoroscopy, iterative algorithms (gradient descent, ADMM) must use the correct forward model.

---

## 36. Mammography (`mammography`)

**Physics**: Mammography uses low-energy X-rays optimized for breast tissue imaging. The Beer-Lambert attenuation model with tissue-specific absorption coefficients produces fundamentally different contrast from Gaussian blur.

**Forward equation**: `y = eta * I_0 * exp(-mu(E) * x) + n`

**Correct operator**: `graph` | **Input shape**: (64, 64) | **Output shape**: (64, 64) | **Linear**: No

### Mismatch Example 1: Forward Pass Comparison

| Metric | Correct | Widefield Fallback |
|--------|---------|-------------------|
| Output shape | (64, 64) | (64, 64) |
| Shapes match | Yes | — |
| Complex output | No | No |
| RMSE | 18241.261834 | — |
| Correlation | -0.2433 | — |
| Max abs diff | 261208.477746 | — |
| Relative RMSE | 1.0000 | — |

**Failure**: While output shapes match, the pixel values are fundamentally different. RMSE = 18241.261834, correlation = -0.2433. The widefield blur applies a generic Gaussian PSF that does not capture the physics of Mammography.

**Correction**: Use the correct `mammography_graph_v2` operator which implements the physical forward model: y = eta * I_0 * exp(-mu(E) * x) + n.

### Mismatch Example 2: Round-Trip Fidelity

| Metric | Correct | Widefield Fallback |
|--------|---------|-------------------|
| Output shape | (64, 64) | (64, 64) |
| Shapes match | Yes | — |

**Failure**: The correct operator's adjoint is not available (non-linear model). The widefield round-trip (blur->blur) simply double-blurs the image, which does not represent the non-linear inverse problem.

**Correction**: For non-linear modalities like Mammography, iterative algorithms (gradient descent, ADMM) must use the correct forward model.

---

## 37. Dual-Energy X-ray Absorptiometry (DEXA) (`dexa`)

**Physics**: DEXA acquires X-ray images at two energies to separate bone and soft tissue contributions. The output is a dual-channel image (2, H, W), fundamentally different from a single-channel blurred image.

**Forward equation**: `y_E = I_0(E) * exp(-(mu_bone*x_bone + mu_soft*x_soft)) + n`

**Correct operator**: `graph` | **Input shape**: (64, 64) | **Output shape**: (2, 64, 64) | **Linear**: No

### Mismatch Example 1: Forward Pass Comparison

| Metric | Correct | Widefield Fallback |
|--------|---------|-------------------|
| Output shape | (2, 64, 64) | (64, 64) |
| Shapes match | **No** | — |
| Complex output | No | No |

**Failure**: The correct forward model produces output of shape (2, 64, 64), but the widefield fallback produces shape (64, 64). Any reconstruction algorithm expecting the correct output shape will crash or produce meaningless results.

**Correction**: Use the correct `dexa_graph_v2` operator which produces the physically valid output shape (2, 64, 64).

### Mismatch Example 2: Round-Trip Fidelity

| Metric | Correct | Widefield Fallback |
|--------|---------|-------------------|
| Output shape | (64, 64) | (64, 64) |
| Shapes match | Yes | — |

**Failure**: The correct operator's adjoint is not available (non-linear model). The widefield round-trip (blur->blur) simply double-blurs the image, which does not represent the non-linear inverse problem.

**Correction**: For non-linear modalities like Dual-Energy X-ray Absorptiometry (DEXA), iterative algorithms (gradient descent, ADMM) must use the correct forward model.

---

## 38. Cone-Beam CT (`cbct`)

**Physics**: Cone-beam CT uses a divergent X-ray beam to acquire projections at multiple angles, producing a sinogram. Like fan-beam CT, the output shape (n_angles, n_detectors) is fundamentally different from the 2D input image.

**Forward equation**: `y(theta, s) = integral x(r) dl (cone-beam Radon)`

**Correct operator**: `graph` | **Input shape**: (64, 64) | **Output shape**: (180, 64) | **Linear**: Yes

> **Build Error**: `RuntimeError: Forward pass failed at node 'noise' (primitive=poisson_only_sensor): lam value too large`

### Mismatch Example 1: Forward Pass Comparison (from metadata)

| Metric | Correct | Widefield Fallback |
|--------|---------|-------------------|
| Output shape | (180, 64) | (64, 64) |
| Shapes match | **No** | — |
| Complex output | No | No |

**Failure**: Operator build failed: Forward pass failed at node 'noise' (primitive=poisson_only_sensor): lam value too large. Based on metadata, correct output shape is (180, 64) vs widefield (64, 64).

**Correction**: Fix the operator build for cbct and re-run.

### Mismatch Example 2: Round-Trip Fidelity (from metadata)

| Metric | Correct | Widefield Fallback |
|--------|---------|-------------------|
| Output shape | (64, 64) | (64, 64) |
| Shapes match | Yes | — |

**Failure**: Operator build failed. Correct x_shape is (64, 64) vs widefield (64, 64).

**Correction**: Fix the operator build for cbct and re-run.

---

## 39. X-ray Angiography (`angiography`)

**Physics**: X-ray angiography visualizes blood vessels using contrast agent injection. The forward model involves Beer-Lambert attenuation with time-dependent contrast agent concentration and temporal subtraction (DSA), unlike simple Gaussian blur.

**Forward equation**: `y = eta * I_0 * exp(-mu_tissue*x - mu_contrast*c(t)) + n`

**Correct operator**: `graph` | **Input shape**: (64, 64) | **Output shape**: (64, 64) | **Linear**: No

### Mismatch Example 1: Forward Pass Comparison

| Metric | Correct | Widefield Fallback |
|--------|---------|-------------------|
| Output shape | (64, 64) | (64, 64) |
| Shapes match | Yes | — |
| Complex output | No | No |
| RMSE | 12071.440744 | — |
| Correlation | -0.2433 | — |
| Max abs diff | 172857.958070 | — |
| Relative RMSE | 1.0000 | — |

**Failure**: While output shapes match, the pixel values are fundamentally different. RMSE = 12071.440744, correlation = -0.2433. The widefield blur applies a generic Gaussian PSF that does not capture the physics of X-ray Angiography.

**Correction**: Use the correct `angiography_graph_v2` operator which implements the physical forward model: y = eta * I_0 * exp(-mu_tissue*x - mu_contrast*c(t)) + n.

### Mismatch Example 2: Round-Trip Fidelity

| Metric | Correct | Widefield Fallback |
|--------|---------|-------------------|
| Output shape | (64, 64) | (64, 64) |
| Shapes match | Yes | — |

**Failure**: The correct operator's adjoint is not available (non-linear model). The widefield round-trip (blur->blur) simply double-blurs the image, which does not represent the non-linear inverse problem.

**Correction**: For non-linear modalities like X-ray Angiography, iterative algorithms (gradient descent, ADMM) must use the correct forward model.

---

## 40. Doppler Ultrasound (`doppler_ultrasound`)

**Physics**: Doppler ultrasound measures blood flow velocity from the frequency shift of reflected ultrasound pulses. The output shape (n_sensors, n_samples) is a 2D matrix of acoustic data, not a spatially blurred image.

**Forward equation**: `f_d = (2 * v * cos(theta) * f_0) / c`

**Correct operator**: `graph` | **Input shape**: (64, 64) | **Output shape**: (32, 64) | **Linear**: No

### Mismatch Example 1: Forward Pass Comparison

| Metric | Correct | Widefield Fallback |
|--------|---------|-------------------|
| Output shape | (32, 64) | (64, 64) |
| Shapes match | **No** | — |
| Complex output | No | No |

**Failure**: The correct forward model produces output of shape (32, 64), but the widefield fallback produces shape (64, 64). Any reconstruction algorithm expecting the correct output shape will crash or produce meaningless results.

**Correction**: Use the correct `doppler_ultrasound_graph_v2` operator which produces the physically valid output shape (32, 64).

### Mismatch Example 2: Round-Trip Fidelity

| Metric | Correct | Widefield Fallback |
|--------|---------|-------------------|
| Output shape | (64, 64) | (64, 64) |
| Shapes match | Yes | — |

**Failure**: The correct operator's adjoint is not available (non-linear model). The widefield round-trip (blur->blur) simply double-blurs the image, which does not represent the non-linear inverse problem.

**Correction**: For non-linear modalities like Doppler Ultrasound, iterative algorithms (gradient descent, ADMM) must use the correct forward model.

---

## 41. Shear-Wave Elastography (`elastography`)

**Physics**: Elastography maps tissue stiffness by tracking shear wave propagation via ultrasonic imaging. The output is acoustic wave data at sensor positions (n_sensors, n_samples), incompatible with 2D blur output.

**Forward equation**: `c_s = sqrt(G / rho), E = 3 * rho * c_s^2`

**Correct operator**: `graph` | **Input shape**: (64, 64) | **Output shape**: (32, 64) | **Linear**: No

### Mismatch Example 1: Forward Pass Comparison

| Metric | Correct | Widefield Fallback |
|--------|---------|-------------------|
| Output shape | (32, 64) | (64, 64) |
| Shapes match | **No** | — |
| Complex output | No | No |

**Failure**: The correct forward model produces output of shape (32, 64), but the widefield fallback produces shape (64, 64). Any reconstruction algorithm expecting the correct output shape will crash or produce meaningless results.

**Correction**: Use the correct `elastography_graph_v2` operator which produces the physically valid output shape (32, 64).

### Mismatch Example 2: Round-Trip Fidelity

| Metric | Correct | Widefield Fallback |
|--------|---------|-------------------|
| Output shape | (64, 64) | (64, 64) |
| Shapes match | Yes | — |

**Failure**: The correct operator's adjoint is not available (non-linear model). The widefield round-trip (blur->blur) simply double-blurs the image, which does not represent the non-linear inverse problem.

**Correction**: For non-linear modalities like Shear-Wave Elastography, iterative algorithms (gradient descent, ADMM) must use the correct forward model.

---

## 42. Functional MRI (BOLD) (`fmri`)

**Physics**: fMRI detects BOLD contrast by measuring T2*-weighted signal changes in k-space. Like standard MRI, data is acquired in the Fourier domain with undersampling, producing complex-valued k-space measurements — not real-valued spatially blurred images.

**Forward equation**: `y = M * F * S * (HRF * x) + n`

**Correct operator**: `graph` | **Input shape**: (64, 64) | **Output shape**: (64, 64) | **Linear**: Yes

### Mismatch Example 1: Forward Pass Comparison

| Metric | Correct | Widefield Fallback |
|--------|---------|-------------------|
| Output shape | (64, 64) | (64, 64) |
| Shapes match | Yes | — |
| Complex output | Yes | No |
| RMSE | 15.671367 | — |
| Correlation | -0.0130 | — |
| Max abs diff | 81.885557 | — |
| Relative RMSE | 1.0009 | — |

**Failure**: The correct forward model produces complex-valued output (contains phase information), but the widefield fallback produces only real-valued output. Phase information is critical for this modality.

**Correction**: Use the correct `fmri_graph_v2` operator which preserves complex-valued output containing both amplitude and phase.

### Mismatch Example 2: Round-Trip Fidelity

| Metric | Correct | Widefield Fallback |
|--------|---------|-------------------|
| Output shape | (64, 64) | (64, 64) |
| Shapes match | Yes | — |

**Failure**: The correct operator's adjoint is not available (non-linear model). The widefield round-trip (blur->blur) simply double-blurs the image, which does not represent the non-linear inverse problem.

**Correction**: For non-linear modalities like Functional MRI (BOLD), iterative algorithms (gradient descent, ADMM) must use the correct forward model.

---

## 43. MR Spectroscopy (`mrs`)

**Physics**: MRS measures the frequency spectrum of metabolites in a localized region. Like MRI, data is acquired in k-space via Fourier encoding. The widefield spatial blur has no concept of spectral information.

**Forward equation**: `S(f) = sum_k a_k * L(f - f_k, T2_k) + n`

**Correct operator**: `graph` | **Input shape**: (64, 64) | **Output shape**: (64, 64) | **Linear**: Yes

### Mismatch Example 1: Forward Pass Comparison

| Metric | Correct | Widefield Fallback |
|--------|---------|-------------------|
| Output shape | (64, 64) | (64, 64) |
| Shapes match | Yes | — |
| Complex output | Yes | No |
| RMSE | 4.757830 | — |
| Correlation | 0.0154 | — |
| Max abs diff | 18.345401 | — |
| Relative RMSE | 1.0035 | — |

**Failure**: The correct forward model produces complex-valued output (contains phase information), but the widefield fallback produces only real-valued output. Phase information is critical for this modality.

**Correction**: Use the correct `mrs_graph_v2` operator which preserves complex-valued output containing both amplitude and phase.

### Mismatch Example 2: Round-Trip Fidelity

| Metric | Correct | Widefield Fallback |
|--------|---------|-------------------|
| Output shape | (64, 64) | (64, 64) |
| Shapes match | Yes | — |

**Failure**: The correct operator's adjoint is not available (non-linear model). The widefield round-trip (blur->blur) simply double-blurs the image, which does not represent the non-linear inverse problem.

**Correction**: For non-linear modalities like MR Spectroscopy, iterative algorithms (gradient descent, ADMM) must use the correct forward model.

---

## 44. Diffusion MRI (DTI) (`diffusion_mri`)

**Physics**: Diffusion MRI measures water molecule diffusion directionality via diffusion-encoding gradients in k-space. Each gradient direction produces a different k-space weighting — the widefield blur ignores all diffusion encoding.

**Forward equation**: `S(g) = S_0 * exp(-b * g^T * D * g) + n`

**Correct operator**: `graph` | **Input shape**: (64, 64) | **Output shape**: (64, 64) | **Linear**: Yes

### Mismatch Example 1: Forward Pass Comparison

| Metric | Correct | Widefield Fallback |
|--------|---------|-------------------|
| Output shape | (64, 64) | (64, 64) |
| Shapes match | Yes | — |
| Complex output | Yes | No |
| RMSE | 38.297383 | — |
| Correlation | -0.0475 | — |
| Max abs diff | 172.372179 | — |
| Relative RMSE | 1.0004 | — |

**Failure**: The correct forward model produces complex-valued output (contains phase information), but the widefield fallback produces only real-valued output. Phase information is critical for this modality.

**Correction**: Use the correct `diffusion_mri_graph_v2` operator which preserves complex-valued output containing both amplitude and phase.

### Mismatch Example 2: Round-Trip Fidelity

| Metric | Correct | Widefield Fallback |
|--------|---------|-------------------|
| Output shape | (64, 64) | (64, 64) |
| Shapes match | Yes | — |

**Failure**: The correct operator's adjoint is not available (non-linear model). The widefield round-trip (blur->blur) simply double-blurs the image, which does not represent the non-linear inverse problem.

**Correction**: For non-linear modalities like Diffusion MRI (DTI), iterative algorithms (gradient descent, ADMM) must use the correct forward model.

---

## 45. Two-Photon Microscopy (`two_photon`)

**Physics**: Two-photon microscopy achieves optical sectioning through non-linear two-photon absorption (signal proportional to I^2). The quadratic dependence on excitation intensity makes the forward model inherently non-linear — unlike the linear Gaussian convolution fallback.

**Forward equation**: `y = PSF ** |x|^2 + n`

**Correct operator**: `graph` | **Input shape**: (64, 64) | **Output shape**: (64, 64) | **Linear**: No

### Mismatch Example 1: Forward Pass Comparison

| Metric | Correct | Widefield Fallback |
|--------|---------|-------------------|
| Output shape | (64, 64) | (64, 64) |
| Shapes match | Yes | — |
| Complex output | No | No |
| RMSE | 0.510099 | — |
| Correlation | -0.0479 | — |
| Max abs diff | 1.550285 | — |
| Relative RMSE | 1.0935 | — |

**Failure**: While output shapes match, the pixel values are fundamentally different. RMSE = 0.510099, correlation = -0.0479. The widefield blur applies a generic Gaussian PSF that does not capture the physics of Two-Photon Microscopy.

**Correction**: Use the correct `two_photon_graph_v2` operator which implements the physical forward model: y = PSF ** |x|^2 + n.

### Mismatch Example 2: Round-Trip Fidelity

| Metric | Correct | Widefield Fallback |
|--------|---------|-------------------|
| Output shape | (64, 64) | (64, 64) |
| Shapes match | Yes | — |

**Failure**: The correct operator's adjoint is not available (non-linear model). The widefield round-trip (blur->blur) simply double-blurs the image, which does not represent the non-linear inverse problem.

**Correction**: For non-linear modalities like Two-Photon Microscopy, iterative algorithms (gradient descent, ADMM) must use the correct forward model.

---

## 46. STED Microscopy (`sted`)

**Physics**: STED achieves sub-diffraction resolution by depleting fluorescence with a donut-shaped depletion beam. The effective PSF is sharpened beyond the diffraction limit — the widefield sigma=2.0 blur dramatically over-blurs compared to the STED effective PSF.

**Forward equation**: `y = PSF_sted ** (x * (1 - eta * donut)) + n`

**Correct operator**: `graph` | **Input shape**: (64, 64) | **Output shape**: (64, 64) | **Linear**: No

### Mismatch Example 1: Forward Pass Comparison

| Metric | Correct | Widefield Fallback |
|--------|---------|-------------------|
| Output shape | (64, 64) | (64, 64) |
| Shapes match | Yes | — |
| Complex output | No | No |
| RMSE | 0.141286 | — |
| Correlation | 0.5128 | — |
| Max abs diff | 0.633215 | — |
| Relative RMSE | 2.1928 | — |

**Failure**: While output shapes match, the pixel values are fundamentally different. RMSE = 0.141286, correlation = 0.5128. The widefield blur applies a generic Gaussian PSF that does not capture the physics of STED Microscopy.

**Correction**: Use the correct `sted_graph_v2` operator which implements the physical forward model: y = PSF_sted ** (x * (1 - eta * donut)) + n.

### Mismatch Example 2: Round-Trip Fidelity

| Metric | Correct | Widefield Fallback |
|--------|---------|-------------------|
| Output shape | (64, 64) | (64, 64) |
| Shapes match | Yes | — |

**Failure**: The correct operator's adjoint is not available (non-linear model). The widefield round-trip (blur->blur) simply double-blurs the image, which does not represent the non-linear inverse problem.

**Correction**: For non-linear modalities like STED Microscopy, iterative algorithms (gradient descent, ADMM) must use the correct forward model.

---

## 47. PALM/STORM Single-Molecule Localization (`palm_storm`)

**Physics**: PALM/STORM achieves nanoscale resolution by stochastically activating sparse subsets of fluorophores and localizing their positions. The forward model involves sparse emitter blinking statistics — fundamentally different from continuous Gaussian convolution.

**Forward equation**: `y_t = sum_k I_k(t) * PSF(r - r_k) + bg + n`

**Correct operator**: `graph` | **Input shape**: (64, 64) | **Output shape**: (64, 64) | **Linear**: No

### Mismatch Example 1: Forward Pass Comparison

| Metric | Correct | Widefield Fallback |
|--------|---------|-------------------|
| Output shape | (64, 64) | (64, 64) |
| Shapes match | Yes | — |
| Complex output | No | No |
| RMSE | 210.493362 | — |
| Correlation | 0.0541 | — |
| Max abs diff | 902.594389 | — |
| Relative RMSE | 1.0000 | — |

**Failure**: While output shapes match, the pixel values are fundamentally different. RMSE = 210.493362, correlation = 0.0541. The widefield blur applies a generic Gaussian PSF that does not capture the physics of PALM/STORM Single-Molecule Localization.

**Correction**: Use the correct `palm_storm_graph_v2` operator which implements the physical forward model: y_t = sum_k I_k(t) * PSF(r - r_k) + bg + n.

### Mismatch Example 2: Round-Trip Fidelity

| Metric | Correct | Widefield Fallback |
|--------|---------|-------------------|
| Output shape | (64, 64) | (64, 64) |
| Shapes match | Yes | — |

**Failure**: The correct operator's adjoint is not available (non-linear model). The widefield round-trip (blur->blur) simply double-blurs the image, which does not represent the non-linear inverse problem.

**Correction**: For non-linear modalities like PALM/STORM Single-Molecule Localization, iterative algorithms (gradient descent, ADMM) must use the correct forward model.

---

## 48. TIRF Microscopy (`tirf`)

**Physics**: Total Internal Reflection Fluorescence (TIRF) selectively excites fluorophores within ~100nm of the coverslip via evanescent wave. The exponential axial decay creates depth sectioning absent in widefield — the uniform blur ignores this axial selectivity.

**Forward equation**: `y = PSF ** (x * exp(-z/d)) + n`

**Correct operator**: `graph` | **Input shape**: (64, 64) | **Output shape**: (64, 64) | **Linear**: Yes

### Mismatch Example 1: Forward Pass Comparison

| Metric | Correct | Widefield Fallback |
|--------|---------|-------------------|
| Output shape | (64, 64) | (64, 64) |
| Shapes match | Yes | — |
| Complex output | No | No |
| RMSE | 0.144145 | — |
| Correlation | 0.7415 | — |
| Max abs diff | 0.619754 | — |
| Relative RMSE | 0.9562 | — |

**Failure**: While output shapes match, the pixel values are fundamentally different. RMSE = 0.144145, correlation = 0.7415. The widefield blur applies a generic Gaussian PSF that does not capture the physics of TIRF Microscopy.

**Correction**: Use the correct `tirf_graph_v2` operator which implements the physical forward model: y = PSF ** (x * exp(-z/d)) + n.

### Mismatch Example 2: Round-Trip Fidelity

| Metric | Correct | Widefield Fallback |
|--------|---------|-------------------|
| Output shape | (64, 64) | (64, 64) |
| Shapes match | Yes | — |

**Failure**: The correct operator's adjoint is not available (non-linear model). The widefield round-trip (blur->blur) simply double-blurs the image, which does not represent the non-linear inverse problem.

**Correction**: For non-linear modalities like TIRF Microscopy, iterative algorithms (gradient descent, ADMM) must use the correct forward model.

---

## 49. Polarization Microscopy (`polarization`)

**Physics**: Polarization microscopy measures birefringence and dichroism via Mueller/Jones matrix formalism. The polarization state transformation is fundamentally different from scalar Gaussian blur — it encodes material anisotropy that the scalar fallback cannot represent.

**Forward equation**: `y = PSF ** (M * x) + n`

**Correct operator**: `graph` | **Input shape**: (64, 64) | **Output shape**: (64, 64) | **Linear**: Yes

### Mismatch Example 1: Forward Pass Comparison

| Metric | Correct | Widefield Fallback |
|--------|---------|-------------------|
| Output shape | (64, 64) | (64, 64) |
| Shapes match | Yes | — |
| Complex output | No | No |
| RMSE | 0.119903 | — |
| Correlation | 0.8244 | — |
| Max abs diff | 0.647605 | — |
| Relative RMSE | 1.2953 | — |

**Failure**: While output shapes match, the pixel values are fundamentally different. RMSE = 0.119903, correlation = 0.8244. The widefield blur applies a generic Gaussian PSF that does not capture the physics of Polarization Microscopy.

**Correction**: Use the correct `polarization_graph_v2` operator which implements the physical forward model: y = PSF ** (M * x) + n.

### Mismatch Example 2: Round-Trip Fidelity

| Metric | Correct | Widefield Fallback |
|--------|---------|-------------------|
| Output shape | (64, 64) | (64, 64) |
| Shapes match | Yes | — |

**Failure**: The correct operator's adjoint is not available (non-linear model). The widefield round-trip (blur->blur) simply double-blurs the image, which does not represent the non-linear inverse problem.

**Correction**: For non-linear modalities like Polarization Microscopy, iterative algorithms (gradient descent, ADMM) must use the correct forward model.

---

## 50. Fiber Bundle Endoscopy (`endoscopy`)

**Physics**: Endoscopic imaging transmits images through a fiber bundle with honeycomb sampling and inter-core crosstalk. The output is a 1D vector of core intensities (n_cores), not a 2D spatially blurred image.

**Forward equation**: `y = Poisson(alpha * S(F * x)) + n`

**Correct operator**: `graph` | **Input shape**: (64, 64) | **Output shape**: (4096) | **Linear**: No

### Mismatch Example 1: Forward Pass Comparison

| Metric | Correct | Widefield Fallback |
|--------|---------|-------------------|
| Output shape | (4096) | (64, 64) |
| Shapes match | **No** | — |
| Complex output | No | No |

**Failure**: The correct forward model produces output of shape (4096,), but the widefield fallback produces shape (64, 64). Any reconstruction algorithm expecting the correct output shape will crash or produce meaningless results.

**Correction**: Use the correct `endoscopy_graph_v2` operator which produces the physically valid output shape (4096,).

### Mismatch Example 2: Round-Trip Fidelity

| Metric | Correct | Widefield Fallback |
|--------|---------|-------------------|
| Output shape | (64, 64) | (64, 64) |
| Shapes match | Yes | — |

**Failure**: The correct operator's adjoint is not available (non-linear model). The widefield round-trip (blur->blur) simply double-blurs the image, which does not represent the non-linear inverse problem.

**Correction**: For non-linear modalities like Fiber Bundle Endoscopy, iterative algorithms (gradient descent, ADMM) must use the correct forward model.

---

## 51. Fundus Camera (`fundus`)

**Physics**: Fundus photography images the retina through the pupil. While similar to widefield in form (PSF blur), the retinal PSF accounts for ocular aberrations and the specific optics of the fundus camera, differing from a generic sigma=2.0 Gaussian.

**Forward equation**: `y = PSF ** x + n`

**Correct operator**: `graph` | **Input shape**: (64, 64) | **Output shape**: (64, 64) | **Linear**: Yes

### Mismatch Example 1: Forward Pass Comparison

| Metric | Correct | Widefield Fallback |
|--------|---------|-------------------|
| Output shape | (64, 64) | (64, 64) |
| Shapes match | Yes | — |
| Complex output | No | No |
| RMSE | 0.123211 | — |
| Correlation | 0.7997 | — |
| Max abs diff | 0.626577 | — |
| Relative RMSE | 1.1594 | — |

**Failure**: While output shapes match, the pixel values are fundamentally different. RMSE = 0.123211, correlation = 0.7997. The widefield blur applies a generic Gaussian PSF that does not capture the physics of Fundus Camera.

**Correction**: Use the correct `fundus_graph_v2` operator which implements the physical forward model: y = PSF ** x + n.

### Mismatch Example 2: Round-Trip Fidelity

| Metric | Correct | Widefield Fallback |
|--------|---------|-------------------|
| Output shape | (64, 64) | (64, 64) |
| Shapes match | Yes | — |

**Failure**: The correct operator's adjoint is not available (non-linear model). The widefield round-trip (blur->blur) simply double-blurs the image, which does not represent the non-linear inverse problem.

**Correction**: For non-linear modalities like Fundus Camera, iterative algorithms (gradient descent, ADMM) must use the correct forward model.

---

## 52. OCT Angiography (`octa`)

**Physics**: OCTA uses repeated OCT scans to detect motion contrast from blood flow. The forward model involves angular spectrum propagation and temporal variance estimation — fundamentally different from single-frame Gaussian blur.

**Forward equation**: `y = Var_t[OCT(x, t)] + n`

**Correct operator**: `graph` | **Input shape**: (64, 64) | **Output shape**: (64, 64) | **Linear**: No

### Mismatch Example 1: Forward Pass Comparison

| Metric | Correct | Widefield Fallback |
|--------|---------|-------------------|
| Output shape | (64, 64) | (64, 64) |
| Shapes match | Yes | — |
| Complex output | No | No |
| RMSE | 0.474457 | — |
| Correlation | -0.0879 | — |
| Max abs diff | 2.439255 | — |
| Relative RMSE | 1.1093 | — |

**Failure**: While output shapes match, the pixel values are fundamentally different. RMSE = 0.474457, correlation = -0.0879. The widefield blur applies a generic Gaussian PSF that does not capture the physics of OCT Angiography.

**Correction**: Use the correct `octa_graph_v2` operator which implements the physical forward model: y = Var_t[OCT(x, t)] + n.

### Mismatch Example 2: Round-Trip Fidelity

| Metric | Correct | Widefield Fallback |
|--------|---------|-------------------|
| Output shape | (64, 64) | (64, 64) |
| Shapes match | Yes | — |

**Failure**: The correct operator's adjoint is not available (non-linear model). The widefield round-trip (blur->blur) simply double-blurs the image, which does not represent the non-linear inverse problem.

**Correction**: For non-linear modalities like OCT Angiography, iterative algorithms (gradient descent, ADMM) must use the correct forward model.

---

## 53. Time-of-Flight Depth Camera (`tof_camera`)

**Physics**: ToF cameras measure scene depth by timing modulated light round-trips. The forward model involves temporal gating and SPAD detection with timing jitter — not spatial Gaussian convolution.

**Forward equation**: `y = Poisson(alpha * G(x)) + n`

**Correct operator**: `graph` | **Input shape**: (64, 64) | **Output shape**: (64, 64) | **Linear**: No

### Mismatch Example 1: Forward Pass Comparison

| Metric | Correct | Widefield Fallback |
|--------|---------|-------------------|
| Output shape | (64, 64) | (64, 64) |
| Shapes match | Yes | — |
| Complex output | No | No |
| RMSE | 0.241801 | — |
| Correlation | 0.2511 | — |
| Max abs diff | 1.057049 | — |
| Relative RMSE | 1.1542 | — |

**Failure**: While output shapes match, the pixel values are fundamentally different. RMSE = 0.241801, correlation = 0.2511. The widefield blur applies a generic Gaussian PSF that does not capture the physics of Time-of-Flight Depth Camera.

**Correction**: Use the correct `tof_camera_graph_v2` operator which implements the physical forward model: y = Poisson(alpha * G(x)) + n.

### Mismatch Example 2: Round-Trip Fidelity

| Metric | Correct | Widefield Fallback |
|--------|---------|-------------------|
| Output shape | (64, 64) | (64, 64) |
| Shapes match | Yes | — |

**Failure**: The correct operator's adjoint is not available (non-linear model). The widefield round-trip (blur->blur) simply double-blurs the image, which does not represent the non-linear inverse problem.

**Correction**: For non-linear modalities like Time-of-Flight Depth Camera, iterative algorithms (gradient descent, ADMM) must use the correct forward model.

---

## 54. LiDAR Scanner (`lidar`)

**Physics**: LiDAR acquires 3D point clouds by scanning a laser beam and timing return pulses. The output is a 1D range profile per scan line — not a 2D spatially blurred image.

**Forward equation**: `y = Poisson(alpha * T(S(x))) + n`

**Correct operator**: `graph` | **Input shape**: (64, 64) | **Output shape**: (64) | **Linear**: No

### Mismatch Example 1: Forward Pass Comparison

| Metric | Correct | Widefield Fallback |
|--------|---------|-------------------|
| Output shape | (64) | (64, 64) |
| Shapes match | **No** | — |
| Complex output | No | No |

**Failure**: The correct forward model produces output of shape (64,), but the widefield fallback produces shape (64, 64). Any reconstruction algorithm expecting the correct output shape will crash or produce meaningless results.

**Correction**: Use the correct `lidar_graph_v2` operator which produces the physically valid output shape (64,).

### Mismatch Example 2: Round-Trip Fidelity

| Metric | Correct | Widefield Fallback |
|--------|---------|-------------------|
| Output shape | (64, 64) | (64, 64) |
| Shapes match | Yes | — |

**Failure**: The correct operator's adjoint is not available (non-linear model). The widefield round-trip (blur->blur) simply double-blurs the image, which does not represent the non-linear inverse problem.

**Correction**: For non-linear modalities like LiDAR Scanner, iterative algorithms (gradient descent, ADMM) must use the correct forward model.

---

## 55. Structured-Light Depth Camera (`structured_light`)

**Physics**: Structured light projects coded fringe patterns onto the scene and triangulates depth from pattern deformation. The projective geometry and fringe analysis are fundamentally different from isotropic Gaussian blur.

**Forward equation**: `y = O(P(x)) + n`

**Correct operator**: `graph` | **Input shape**: (64, 64) | **Output shape**: (64, 64) | **Linear**: No

### Mismatch Example 1: Forward Pass Comparison

| Metric | Correct | Widefield Fallback |
|--------|---------|-------------------|
| Output shape | (64, 64) | (64, 64) |
| Shapes match | Yes | — |
| Complex output | No | No |
| RMSE | 0.354490 | — |
| Correlation | 0.2043 | — |
| Max abs diff | 2.568245 | — |
| Relative RMSE | 1.0448 | — |

**Failure**: While output shapes match, the pixel values are fundamentally different. RMSE = 0.354490, correlation = 0.2043. The widefield blur applies a generic Gaussian PSF that does not capture the physics of Structured-Light Depth Camera.

**Correction**: Use the correct `structured_light_graph_v2` operator which implements the physical forward model: y = O(P(x)) + n.

### Mismatch Example 2: Round-Trip Fidelity

| Metric | Correct | Widefield Fallback |
|--------|---------|-------------------|
| Output shape | (64, 64) | (64, 64) |
| Shapes match | Yes | — |

**Failure**: The correct operator's adjoint is not available (non-linear model). The widefield round-trip (blur->blur) simply double-blurs the image, which does not represent the non-linear inverse problem.

**Correction**: For non-linear modalities like Structured-Light Depth Camera, iterative algorithms (gradient descent, ADMM) must use the correct forward model.

---

## 56. Synthetic Aperture Radar (`sar`)

**Physics**: SAR synthesizes a large aperture by coherently combining radar echoes along the flight path. The complex-valued range-azimuth focusing is fundamentally different from real-valued Gaussian blur. SAR images contain speckle and phase information absent in optical images.

**Forward equation**: `y = A_sar * x + n`

**Correct operator**: `graph` | **Input shape**: (64, 64) | **Output shape**: (64, 64) | **Linear**: Yes

### Mismatch Example 1: Forward Pass Comparison

| Metric | Correct | Widefield Fallback |
|--------|---------|-------------------|
| Output shape | (64, 64) | (64, 64) |
| Shapes match | Yes | — |
| Complex output | Yes | No |
| RMSE | 6.295367 | — |
| Correlation | 0.0080 | — |
| Max abs diff | 21.321481 | — |
| Relative RMSE | 1.0026 | — |

**Failure**: The correct forward model produces complex-valued output (contains phase information), but the widefield fallback produces only real-valued output. Phase information is critical for this modality.

**Correction**: Use the correct `sar_graph_v2` operator which preserves complex-valued output containing both amplitude and phase.

### Mismatch Example 2: Round-Trip Fidelity

| Metric | Correct | Widefield Fallback |
|--------|---------|-------------------|
| Output shape | (64, 64) | (64, 64) |
| Shapes match | Yes | — |

**Failure**: The correct operator's adjoint is not available (non-linear model). The widefield round-trip (blur->blur) simply double-blurs the image, which does not represent the non-linear inverse problem.

**Correction**: For non-linear modalities like Synthetic Aperture Radar, iterative algorithms (gradient descent, ADMM) must use the correct forward model.

---

## 57. Sonar Imaging (`sonar`)

**Physics**: Sonar forms images by beamforming acoustic echoes from an array of transducer elements. The output is a 1D range profile per beam — not a 2D spatially blurred image.

**Forward equation**: `y = B * H * x + n`

**Correct operator**: `graph` | **Input shape**: (64, 64) | **Output shape**: (64) | **Linear**: Yes

### Mismatch Example 1: Forward Pass Comparison

| Metric | Correct | Widefield Fallback |
|--------|---------|-------------------|
| Output shape | (64) | (64, 64) |
| Shapes match | **No** | — |
| Complex output | No | No |

**Failure**: The correct forward model produces output of shape (64,), but the widefield fallback produces shape (64, 64). Any reconstruction algorithm expecting the correct output shape will crash or produce meaningless results.

**Correction**: Use the correct `sonar_graph_v2` operator which produces the physically valid output shape (64,).

### Mismatch Example 2: Round-Trip Fidelity

| Metric | Correct | Widefield Fallback |
|--------|---------|-------------------|
| Output shape | (64, 64) | (64, 64) |
| Shapes match | Yes | — |

**Failure**: The correct operator's adjoint is not available (non-linear model). The widefield round-trip (blur->blur) simply double-blurs the image, which does not represent the non-linear inverse problem.

**Correction**: For non-linear modalities like Sonar Imaging, iterative algorithms (gradient descent, ADMM) must use the correct forward model.

---

## 58. 4D-STEM Electron Diffraction (`electron_diffraction`)

**Physics**: 4D-STEM records a diffraction pattern at each probe position. The far-field diffraction pattern is the squared Fourier modulus of the transmitted wave — a non-linear operation that the linear Gaussian blur cannot represent.

**Forward equation**: `y = |FFT(t * P)|^2 + n`

**Correct operator**: `graph` | **Input shape**: (64, 64) | **Output shape**: (64, 64) | **Linear**: No

### Mismatch Example 1: Forward Pass Comparison

| Metric | Correct | Widefield Fallback |
|--------|---------|-------------------|
| Output shape | (64, 64) | (64, 64) |
| Shapes match | Yes | — |
| Complex output | No | No |
| RMSE | 3868.463018 | — |
| Correlation | -0.0279 | — |
| Max abs diff | 17693.787895 | — |
| Relative RMSE | 1.0000 | — |

**Failure**: While output shapes match, the pixel values are fundamentally different. RMSE = 3868.463018, correlation = -0.0279. The widefield blur applies a generic Gaussian PSF that does not capture the physics of 4D-STEM Electron Diffraction.

**Correction**: Use the correct `electron_diffraction_graph_v2` operator which implements the physical forward model: y = |FFT(t * P)|^2 + n.

### Mismatch Example 2: Round-Trip Fidelity

| Metric | Correct | Widefield Fallback |
|--------|---------|-------------------|
| Output shape | (64, 64) | (64, 64) |
| Shapes match | Yes | — |

**Failure**: The correct operator's adjoint is not available (non-linear model). The widefield round-trip (blur->blur) simply double-blurs the image, which does not represent the non-linear inverse problem.

**Correction**: For non-linear modalities like 4D-STEM Electron Diffraction, iterative algorithms (gradient descent, ADMM) must use the correct forward model.

---

## 59. Electron Backscatter Diffraction (`ebsd`)

**Physics**: EBSD measures crystallographic orientation by analyzing Kikuchi diffraction patterns from backscattered electrons. The reciprocal-space geometry of the diffraction patterns is unrelated to simple spatial Gaussian blur.

**Forward equation**: `y = R(theta,phi,psi) * I_ref + n`

**Correct operator**: `graph` | **Input shape**: (64, 64) | **Output shape**: (64, 64) | **Linear**: No

### Mismatch Example 1: Forward Pass Comparison

| Metric | Correct | Widefield Fallback |
|--------|---------|-------------------|
| Output shape | (64, 64) | (64, 64) |
| Shapes match | Yes | — |
| Complex output | No | No |
| RMSE | 0.293822 | — |
| Correlation | -0.0147 | — |
| Max abs diff | 2.235333 | — |
| Relative RMSE | 1.2097 | — |

**Failure**: While output shapes match, the pixel values are fundamentally different. RMSE = 0.293822, correlation = -0.0147. The widefield blur applies a generic Gaussian PSF that does not capture the physics of Electron Backscatter Diffraction.

**Correction**: Use the correct `ebsd_graph_v2` operator which implements the physical forward model: y = R(theta,phi,psi) * I_ref + n.

### Mismatch Example 2: Round-Trip Fidelity

| Metric | Correct | Widefield Fallback |
|--------|---------|-------------------|
| Output shape | (64, 64) | (64, 64) |
| Shapes match | Yes | — |

**Failure**: The correct operator's adjoint is not available (non-linear model). The widefield round-trip (blur->blur) simply double-blurs the image, which does not represent the non-linear inverse problem.

**Correction**: For non-linear modalities like Electron Backscatter Diffraction, iterative algorithms (gradient descent, ADMM) must use the correct forward model.

---

## 60. Electron Energy Loss Spectroscopy (`eels`)

**Physics**: EELS measures the energy distribution of electrons transmitted through a thin specimen. The forward model convolves the energy loss spectrum with the zero-loss peak — a spectral convolution distinct from spatial Gaussian blur.

**Forward equation**: `y = ZLP ** S(E) + n`

**Correct operator**: `graph` | **Input shape**: (64, 64) | **Output shape**: (64, 64) | **Linear**: Yes

### Mismatch Example 1: Forward Pass Comparison

| Metric | Correct | Widefield Fallback |
|--------|---------|-------------------|
| Output shape | (64, 64) | (64, 64) |
| Shapes match | Yes | — |
| Complex output | No | No |
| RMSE | 0.489338 | — |
| Correlation | 0.2531 | — |
| Max abs diff | 2.302491 | — |
| Relative RMSE | 1.0060 | — |

**Failure**: While output shapes match, the pixel values are fundamentally different. RMSE = 0.489338, correlation = 0.2531. The widefield blur applies a generic Gaussian PSF that does not capture the physics of Electron Energy Loss Spectroscopy.

**Correction**: Use the correct `eels_graph_v2` operator which implements the physical forward model: y = ZLP ** S(E) + n.

### Mismatch Example 2: Round-Trip Fidelity

| Metric | Correct | Widefield Fallback |
|--------|---------|-------------------|
| Output shape | (64, 64) | (64, 64) |
| Shapes match | Yes | — |

**Failure**: The correct operator's adjoint is not available (non-linear model). The widefield round-trip (blur->blur) simply double-blurs the image, which does not represent the non-linear inverse problem.

**Correction**: For non-linear modalities like Electron Energy Loss Spectroscopy, iterative algorithms (gradient descent, ADMM) must use the correct forward model.

---

## 61. Electron Holography (`electron_holography`)

**Physics**: Electron holography records the interference between the transmitted electron wave and a reference wave, encoding phase information. The complex-valued interference pattern contains electromagnetic potential information absent in real-valued Gaussian blur.

**Forward equation**: `y = |psi_obj + psi_ref * exp(i*q*x)|^2 + n`

**Correct operator**: `graph` | **Input shape**: (64, 64) | **Output shape**: (64, 64) | **Linear**: No

### Mismatch Example 1: Forward Pass Comparison

| Metric | Correct | Widefield Fallback |
|--------|---------|-------------------|
| Output shape | (64, 64) | (64, 64) |
| Shapes match | Yes | — |
| Complex output | No | No |
| RMSE | 0.427469 | — |
| Correlation | 0.0371 | — |
| Max abs diff | 1.842187 | — |
| Relative RMSE | 1.0873 | — |

**Failure**: While output shapes match, the pixel values are fundamentally different. RMSE = 0.427469, correlation = 0.0371. The widefield blur applies a generic Gaussian PSF that does not capture the physics of Electron Holography.

**Correction**: Use the correct `electron_holography_graph_v2` operator which implements the physical forward model: y = |psi_obj + psi_ref * exp(i*q*x)|^2 + n.

### Mismatch Example 2: Round-Trip Fidelity

| Metric | Correct | Widefield Fallback |
|--------|---------|-------------------|
| Output shape | (64, 64) | (64, 64) |
| Shapes match | Yes | — |

**Failure**: The correct operator's adjoint is not available (non-linear model). The widefield round-trip (blur->blur) simply double-blurs the image, which does not represent the non-linear inverse problem.

**Correction**: For non-linear modalities like Electron Holography, iterative algorithms (gradient descent, ADMM) must use the correct forward model.

---

## 62. Neutron Radiography/Tomography (`neutron_tomo`)

**Physics**: Neutron tomography uses neutron beams that are attenuated by nuclear interactions (complementary to X-ray contrast). The Beer-Lambert attenuation with neutron cross-sections produces different contrast from optical Gaussian blur.

**Forward equation**: `y = I_0 * exp(-sigma * x) + n`

**Correct operator**: `graph` | **Input shape**: (64, 64) | **Output shape**: (64, 64) | **Linear**: No

### Mismatch Example 1: Forward Pass Comparison

| Metric | Correct | Widefield Fallback |
|--------|---------|-------------------|
| Output shape | (64, 64) | (64, 64) |
| Shapes match | Yes | — |
| Complex output | No | No |
| RMSE | 5017.327371 | — |
| Correlation | -0.3009 | — |
| Max abs diff | 6000.115562 | — |
| Relative RMSE | 1.0000 | — |

**Failure**: While output shapes match, the pixel values are fundamentally different. RMSE = 5017.327371, correlation = -0.3009. The widefield blur applies a generic Gaussian PSF that does not capture the physics of Neutron Radiography/Tomography.

**Correction**: Use the correct `neutron_tomo_graph_v2` operator which implements the physical forward model: y = I_0 * exp(-sigma * x) + n.

### Mismatch Example 2: Round-Trip Fidelity

| Metric | Correct | Widefield Fallback |
|--------|---------|-------------------|
| Output shape | (64, 64) | (64, 64) |
| Shapes match | Yes | — |

**Failure**: The correct operator's adjoint is not available (non-linear model). The widefield round-trip (blur->blur) simply double-blurs the image, which does not represent the non-linear inverse problem.

**Correction**: For non-linear modalities like Neutron Radiography/Tomography, iterative algorithms (gradient descent, ADMM) must use the correct forward model.

---

## 63. Proton Radiography (`proton_radiography`)

**Physics**: Proton radiography measures the attenuation and multiple Coulomb scattering of proton beams through matter. The scattering kernel (MCS blur) and exponential attenuation are fundamentally different from a simple Gaussian PSF.

**Forward equation**: `y = K_mcs ** (I_0 * exp(-sigma * x)) + n`

**Correct operator**: `graph` | **Input shape**: (64, 64) | **Output shape**: (64, 64) | **Linear**: No

### Mismatch Example 1: Forward Pass Comparison

| Metric | Correct | Widefield Fallback |
|--------|---------|-------------------|
| Output shape | (64, 64) | (64, 64) |
| Shapes match | Yes | — |
| Complex output | No | No |
| RMSE | 10010.494335 | — |
| Correlation | -0.9623 | — |
| Max abs diff | 10236.763705 | — |
| Relative RMSE | 1.0000 | — |

**Failure**: While output shapes match, the pixel values are fundamentally different. RMSE = 10010.494335, correlation = -0.9623. The widefield blur applies a generic Gaussian PSF that does not capture the physics of Proton Radiography.

**Correction**: Use the correct `proton_radiography_graph_v2` operator which implements the physical forward model: y = K_mcs ** (I_0 * exp(-sigma * x)) + n.

### Mismatch Example 2: Round-Trip Fidelity

| Metric | Correct | Widefield Fallback |
|--------|---------|-------------------|
| Output shape | (64, 64) | (64, 64) |
| Shapes match | Yes | — |

**Failure**: The correct operator's adjoint is not available (non-linear model). The widefield round-trip (blur->blur) simply double-blurs the image, which does not represent the non-linear inverse problem.

**Correction**: For non-linear modalities like Proton Radiography, iterative algorithms (gradient descent, ADMM) must use the correct forward model.

---

## 64. Muon Tomography (`muon_tomo`)

**Physics**: Muon tomography tracks cosmic-ray muons through objects, using scattering angle distributions to reconstruct density. The output is scattering data at detector positions — not a spatially blurred image.

**Forward equation**: `theta_rms = (13.6/p) * sqrt(L/X_0) + n`

**Correct operator**: `graph` | **Input shape**: (64, 64) | **Output shape**: (64, 64) | **Linear**: No

### Mismatch Example 1: Forward Pass Comparison

| Metric | Correct | Widefield Fallback |
|--------|---------|-------------------|
| Output shape | (64, 64) | (64, 64) |
| Shapes match | Yes | — |
| Complex output | No | No |
| RMSE | 0.119268 | — |
| Correlation | 0.8296 | — |
| Max abs diff | 0.601520 | — |
| Relative RMSE | 1.2953 | — |

**Failure**: While output shapes match, the pixel values are fundamentally different. RMSE = 0.119268, correlation = 0.8296. The widefield blur applies a generic Gaussian PSF that does not capture the physics of Muon Tomography.

**Correction**: Use the correct `muon_tomo_graph_v2` operator which implements the physical forward model: theta_rms = (13.6/p) * sqrt(L/X_0) + n.

### Mismatch Example 2: Round-Trip Fidelity

| Metric | Correct | Widefield Fallback |
|--------|---------|-------------------|
| Output shape | (64, 64) | (64, 64) |
| Shapes match | Yes | — |

**Failure**: The correct operator's adjoint is not available (non-linear model). The widefield round-trip (blur->blur) simply double-blurs the image, which does not represent the non-linear inverse problem.

**Correction**: For non-linear modalities like Muon Tomography, iterative algorithms (gradient descent, ADMM) must use the correct forward model.

---
