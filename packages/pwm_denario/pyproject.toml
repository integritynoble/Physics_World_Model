[project]
name = "pwm_denario"
version = "0.2.1"
description = "Denario adapter for PWM (thin wrapper; optional agent nodes)"
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
  "pwm_core==0.2.1",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
