# PWM Denario

Denario adapter layer for PWM (Physics World Model).

This is a thin wrapper providing integration with autonomous agents.

## Installation

```bash
pip install -e packages/pwm_denario
```

## Dependencies

- pwm_core (installed automatically)
