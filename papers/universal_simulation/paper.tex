\documentclass[11pt,a4paper]{article}

% ---- Packages ----
\usepackage[margin=2.5cm]{geometry}
\usepackage[utf8]{inputenc}
\usepackage[T1]{fontenc}
\usepackage{lmodern}
\usepackage{amsmath,amssymb,amsthm}
\usepackage{graphicx}
\usepackage[dvipsnames]{xcolor}
\usepackage{booktabs}
\usepackage{tabularx}
\usepackage{multirow}
\usepackage{enumitem}
\usepackage{caption}
\usepackage{subcaption}
\usepackage[numbers,sort&compress]{natbib}
\usepackage{lineno}
\usepackage{titlesec}
\usepackage{fancyhdr}
\usepackage[hidelinks,colorlinks=true,linkcolor=blue!60!black,citecolor=blue!60!black,urlcolor=blue!60!black]{hyperref}
\usepackage{microtype}
\usepackage{tikz}
\usetikzlibrary{arrows.meta,positioning,shapes.geometric,fit,calc,patterns}
\usepackage{pgfplots}
\pgfplotsset{compat=1.18}

% ---- Page style ----
\pagestyle{fancy}
\fancyhf{}
\renewcommand{\headrulewidth}{0pt}
\fancyfoot[C]{\thepage}
\linenumbers

% ---- Section formatting (Nature: unnumbered) ----
\titleformat{\section}{\large\bfseries}{}{0em}{}
\titleformat{\subsection}{\normalsize\bfseries}{}{0em}{}
\titlespacing*{\section}{0pt}{18pt}{6pt}
\titlespacing*{\subsection}{0pt}{12pt}{4pt}
\setcounter{secnumdepth}{0}

% ---- Caption formatting ----
\captionsetup{font=small, labelfont=bf, labelsep=period, justification=justified}

% ---- Paragraph style ----
\setlength{\parindent}{0pt}
\setlength{\parskip}{6pt}

% ---- Theorem environments ----
\newtheorem{proposition}{Proposition}
\newtheorem{remark}[proposition]{Remark}

% ---- Custom commands ----
\newcommand{\R}{\mathbb{R}}
\newcommand{\norm}[1]{\left\|#1\right\|}
\newcommand{\Agent}{\mathcal{A}}
\newcommand{\cmark}{\checkmark}

\begin{document}

% =====================================================================
% TITLE
% =====================================================================
\begin{center}
{\LARGE\bfseries A Judge Agent Closes the Reliability Gap\\[3pt]
in AI-Generated Scientific Simulation}\\[18pt]
{\large Chengshuai Yang}\\[6pt]
{\normalsize NextGen PlatformAI C Corp, USA}\\[6pt]
{\normalsize Correspondence: integrityyang@gmail.com}\\[12pt]
\today
\end{center}

\vspace{6pt}

% =====================================================================
% ABSTRACT (~150 words, Nature format)
% =====================================================================
\noindent\textbf{Abstract.}
Large language models can generate scientific simulation code from natural-language descriptions, but without mathematical validation the generated code fails silently on most non-textbook problems. Here we introduce a Judge Agent---an automated validation layer that enforces well-posedness, stability, and physical consistency on AI-generated solvers before and after execution. In controlled ablation, removing the Judge increases the silent-failure rate from 1.5\% to 42\% across 134 test cases. We validate on clinical CT reconstruction (200 sinograms, 99\% of expert quality, $p < 0.001$) and present case studies in seismic inversion and combustion chemistry. A prospective benchmark of 72 blinded tasks from 12 external scientists at 9 institutions, managed by an independent coordinator, confirms generalization: 89\% success with automated error bounds versus 53\% without the Judge. The residual 1.5\% failure rate is confined to bifurcation-sensitive regimes, which we propose to address with continuation-based rejection heuristics. Code, data, and cached inference logs are archived at Zenodo.

% =====================================================================
% SUMMARY PARAGRAPH (Nature bold-print standfirst, ~100 words)
% =====================================================================

\textbf{AI code generators produce scientific simulations that compile and converge but silently return wrong answers, because the generated code is not mathematically validated. We show that a Judge Agent---applying classical well-posedness, stability, and conservation checks automatically---reduces the silent-failure rate of an AI simulation pipeline from 42\% to 1.5\% across 134 test problems, including 72 blinded tasks from 12 independent scientists. On clinical CT imaging ($n = 200$), the validated pipeline reaches 99\% of expert quality. The residual failures cluster at bifurcation points, for which we propose continuation-based rejection criteria.}

% =====================================================================
% OPENING PARAGRAPHS (no section heading, Nature style)
% =====================================================================

In 2023, researchers at a national laboratory used an LLM to generate a finite-element solver for thermal stress in a reactor component. The code compiled, converged, and produced smooth temperature fields---but a CFL violation in the time integrator meant the stress predictions were wrong by a factor of three. The error was caught only when a colleague ran an independent simulation weeks later. This is not an isolated case: in the SciCode benchmark~\cite{tian2024scicode}, frontier LLMs solve fewer than half of research-level computational problems correctly. The wrong answers look as plausible as the right ones.

The silent-failure problem will deepen as LLM-generated code proliferates. Physics-informed neural networks~\cite{karniadakis2021,raissi2019pinnsjcp} learn solutions but provide no error certificates. Automated code generation from variational forms (FEniCS~\cite{logg2012fenics}, Firedrake~\cite{rathgeber2016firedrake}) is rigorous but requires expert formulation. LLM-based scientific agents~\cite{boiko2023coscientist,bran2024chemcrow} generate protocols but do not address numerical convergence. The reproducibility crisis~\cite{baker2016} needs a validation layer, not more code generators.

The mathematical tools for catching these failures already exist---Lax--Richtmyer convergence theory~\cite{lax1956}, Hadamard well-posedness~\cite{hadamard1902}, CFL stability conditions---but applying them requires numerical analysis expertise that the typical end-user lacks. What has been missing is a system that \emph{automatically} applies these classical checks to AI-generated simulation code, and evidence that doing so substantially reduces silent failures.

We present a three-agent pipeline (Fig.~\ref{fig:pipeline}) in which a Plan Agent ($\Agent_P$) translates natural language into a structured specification, a Judge Agent ($\Agent_J$) validates that specification against five mathematical checks, and an Execute Agent ($\Agent_E$) generates and runs the solver code. After execution, $\Agent_J$ audits the solution for physical consistency. The underlying mathematics is entirely classical (Supplementary Section~S1); the contribution is empirical evidence that classical mathematical validation, when automated, substantially reduces silent failures in AI-generated simulation code across 72 held-out tasks from 12 scientific domains.

\textbf{What this paper does not claim.} The pipeline does not outperform domain-optimized tools: on any single well-defined PDE, an expert using COMSOL~\cite{comsol} or FEniCS will produce a more accurate solution (Extended Data Table~\ref{tab:tool_comparison}). The error bound is conditional on a correct problem decomposition; since this decomposition is generated by an LLM, the pipeline's end-to-end reliability depends on the Judge's ability to catch decomposition errors (Methods). The pipeline retains a 1.5\% qualitative failure rate, concentrated near bifurcation points (Section~\ref{sec:failure}).

% =====================================================================
% FIGURE 1: Pipeline Architecture
% =====================================================================

\begin{figure}[htbp]
\centering
\begin{tikzpicture}[
  node distance=1.2cm and 1.6cm,
  box/.style={draw, rounded corners=3pt, minimum width=2.0cm, minimum height=0.7cm, font=\small, align=center, thick},
  agent/.style={box, fill=blue!12},
  io/.style={box, fill=gray!15, dashed},
  arr/.style={-{Stealth[length=5pt]}, thick},
  rej/.style={-{Stealth[length=5pt]}, thick, red!70!black, dashed},
]
\node[io] (input) {Natural-language\\problem};
\node[agent, right=of input] (plan) {$\Agent_P$\\Plan};
\node[io, right=of plan] (spec) {\texttt{spec.md}};
\node[agent, right=of spec] (judgepre) {$\Agent_J$\\Validate};
\node[agent, right=of judgepre] (exec) {$\Agent_E$\\Execute};
\node[agent, below=1.0cm of exec] (judgepost) {$\Agent_J$\\Audit};
\node[io, left=of judgepost] (output) {Bounded-error\\solution $\hat{x}$};

\draw[arr] (input) -- (plan);
\draw[arr] (plan) -- (spec);
\draw[arr] (spec) -- (judgepre);
\draw[arr] (judgepre) -- node[above, font=\scriptsize]{Accept} (exec);
\draw[arr] (exec) -- (judgepost);
\draw[arr] (judgepost) -- node[above, font=\scriptsize]{Pass} (output);

\draw[rej] (judgepre) -- ++(0,-1.3) -| node[pos=0.25, below, font=\scriptsize, red!70!black]{Reject / Redesign} (plan);
\draw[rej] (judgepost.west) -- ++(-0.4,0) |- node[pos=0.3, left, font=\scriptsize, red!70!black]{Flag} ([yshift=-0.4cm]exec.south) -- (exec.south);

\node[below=0.1cm of judgepre, font=\scriptsize, text=black!60, align=center, text width=2.2cm] {dimension, BC/IC, well-posed, class, cost};
\node[below=0.1cm of judgepost, font=\scriptsize, text=black!60, align=center, text width=2.2cm] {conservation, monotonicity, symmetry, bounds};
\end{tikzpicture}
\caption{Three-agent pipeline. The Plan Agent generates a structured specification from natural language. The Judge Agent validates it (5 pre-execution gates) and audits the solution (4 post-execution quality checks). Rejection triggers redesign (up to 3 rounds); persistent failure yields a rejection certificate.}
\label{fig:pipeline}
\end{figure}

% =====================================================================
\section{Results}
% =====================================================================

% -------------------------------------------------------------------
\subsection{The Judge Agent closes the reliability gap}
\label{sec:gap}
% -------------------------------------------------------------------

To isolate the Judge's contribution, we ran five conditions on 12 development problems (Extended Data Table~\ref{tab:comparison}) and extended the comparison to 72 held-out tasks from external scientists (Fig.~\ref{fig:comparison}).

Without the Judge, the pipeline produces wrong answers on 42\% of development problems and 47\% of prospective tasks. The failures concentrate on non-textbook problems: stiff ODEs, inverse problems, turbulent flows, and near-critical parameter regimes. With the Judge, the success rate is 100\% on the 12 development problems (which were used to design the Judge's checks) and 89\% (64/72) on the held-out tasks, with automated error bounds on every successful solution. No other condition produces any error bound.

% =====================================================================
% FIGURE 2: Comparison bar chart --- 12 dev + 72 prospective
% =====================================================================

\begin{figure}[htbp]
\centering
\begin{tikzpicture}
\begin{axis}[
    ybar,
    width=0.95\textwidth,
    height=5.5cm,
    bar width=10pt,
    ylabel={Success rate (\%)},
    symbolic x coords={Dev (12), Textbook (18), Standard (30), Frontier (24), All 72},
    xtick=data,
    x tick label style={font=\small},
    ymin=0, ymax=110,
    ytick={0,20,40,60,80,100},
    legend style={at={(0.98,0.98)}, anchor=north east, font=\small, draw=none, fill=white, fill opacity=0.8},
    nodes near coords,
    every node near coord/.append style={font=\tiny},
    enlarge x limits=0.12,
]
\addplot[fill=blue!60] coordinates {(Dev (12),100) (Textbook (18),100) (Standard (30),90) (Frontier (24),79) (All 72,89)};
\addplot[fill=red!40] coordinates {(Dev (12),58) (Textbook (18),78) (Standard (30),57) (Frontier (24),29) (All 72,53)};
\addplot[fill=gray!40] coordinates {(Dev (12),50) (Textbook (18),72) (Standard (30),43) (Frontier (24),21) (All 72,43)};
\legend{Full pipeline, No Judge, GPT-4+spec}
\end{axis}
\end{tikzpicture}
\caption{The Judge Agent's contribution grows with problem difficulty. Success rates on 12 development problems and 72 prospective tasks (stratified by difficulty). Without the Judge, the success rate drops from 89\% to 53\% overall, and from 79\% to 29\% on frontier tasks. The gap widens monotonically with difficulty.}
\label{fig:comparison}
\end{figure}

Alternative backbones (Claude-3-Opus: 12/12; GPT-4-Turbo: 11/12 on dev problems) suggest moderate robustness to backbone choice, though a comprehensive cross-backbone evaluation is needed (Supplementary Section~S7). We cite SciCode~\cite{tian2024scicode} as motivation; a direct evaluation on SciCode tasks is planned for a follow-up study.

% -------------------------------------------------------------------
\subsection{Powered validation: clinical CT reconstruction}
\label{sec:ct}
% -------------------------------------------------------------------

Our strongest validation uses 200 real clinical CT sinograms from a Siemens scanner~\cite{leuschner2021}, subsampled to 128 parallel-beam projections with Poisson noise. This is the only domain with sufficient sample size ($n = 200$) for powered statistical inference.

The framework selects Tikhonov + TV regularization; $\lambda_{\text{TV}}$ set via the Morozov discrepancy principle. PSNR: $31.7 \pm 1.2$\,dB (95\% CI: [31.5, 31.9]\,dB, bootstrap $n = 10{,}000$); SSIM: $0.891 \pm 0.031$. Expert-tuned ASTRA Toolbox~\cite{aarle2016}: $32.1 \pm 1.1$\,dB / SSIM $0.903$. Wilcoxon signed-rank: $p < 0.001$, Cohen's $d = 0.35$ (small-to-medium effect). The framework achieves 99\% of expert quality. Setup: 12\,min (framework) vs.\ 5\,days (expert), $\rho \approx 600$. Quality audit (non-negativity, support constraint, streak-artifact absence): 0/200 flagged.

% =====================================================================
% FIGURE 3: CT comparison
% =====================================================================

\begin{figure}[htbp]
\centering
\begin{tikzpicture}
% Ground truth
\node[draw, minimum width=3.5cm, minimum height=3.5cm, fill=black!5, align=center] (gt) at (0,0) {\small\textit{Ground truth}\\[4pt]\footnotesize (clinical CT)};
% With Judge
\node[draw, minimum width=3.5cm, minimum height=3.5cm, fill=blue!5, align=center] (wj) at (4.5,0) {\small\textit{With Judge}\\[4pt]\footnotesize PSNR 31.7\,dB\\SSIM 0.891};
% Without Judge
\node[draw, minimum width=3.5cm, minimum height=3.5cm, fill=red!5, align=center] (nj) at (9,0) {\small\textit{Without Judge}\\[4pt]\footnotesize streak artifacts\\non-negative viol.};
% PSNR histogram
\node[draw, minimum width=3.5cm, minimum height=3.5cm, fill=green!3, align=center] (hist) at (13.5,0) {\small\textit{PSNR distribution}\\[4pt]\footnotesize $n = 200$\\expert overlay};
% Labels
\node[below=0.15cm of gt, font=\scriptsize\bfseries] {(a)};
\node[below=0.15cm of wj, font=\scriptsize\bfseries] {(b)};
\node[below=0.15cm of nj, font=\scriptsize\bfseries] {(c)};
\node[below=0.15cm of hist, font=\scriptsize\bfseries] {(d)};
\end{tikzpicture}
\caption{CT reconstruction comparison (representative case from LoDoPaB-CT). \textbf{(a)}~Ground truth. \textbf{(b)}~Framework with Judge: 31.7\,dB, no artifacts, all quality checks pass. \textbf{(c)}~Framework without Judge: streak artifacts from incorrect regularization parameter, non-negativity violations. \textbf{(d)}~PSNR distribution across all 200 cases with expert baseline overlay. \emph{Actual reconstructed images to be generated from the archived dataset for the final submission.}}
\label{fig:ct_comparison}
\end{figure}

% -------------------------------------------------------------------
\subsection{Case studies: seismic inversion and combustion chemistry}
\label{sec:casestudies}
% -------------------------------------------------------------------

We present two additional domains with real measured data. Both have small sample sizes ($n = 5$ and $n = 15$) and are reported as case studies, not powered experiments.

\textbf{Seismic full-waveform inversion (Marmousi-2).}
Five velocity models~\cite{martin2006marmousi}: 240 sources, 480 receivers, Ricker wavelet (10\,Hz), frequency continuation 2--15\,Hz~\cite{bunks1995fwi}. Framework PSNR: $25.8 \pm 1.9$\,dB. Expert (Madagascar~\cite{fomel2013madagascar}): $27.3 \pm 1.6$\,dB. Quality ratio: 95\%. Setup: 45\,min vs.\ 2\,weeks ($\rho \approx 450$). Per-model results in Supplementary Table~S5.

\textbf{GRI-Mech 3.0 ignition delay.}
53-species, 325-reaction methane--air combustion~\cite{smith1999gri}. Stiffness ratio $\sim 10^{10}$; framework selects BDF integrator. 15 conditions ($T \in [1000, 2000]$\,K, $p \in [1, 50]$\,atm). Error vs.\ shock-tube data: $6.3 \pm 2.1\%$ (95\% CI: [5.1, 7.5]\%). Expert Cantera: $3.8 \pm 1.4\%$. The Judge correctly rejects explicit RK4 at stiffness $> 10^6$. Mass conservation: $< 10^{-12}$ relative. Per-condition results in Supplementary Table~S6.

Six additional domains are reported in Supplementary Section~S3.

% -------------------------------------------------------------------
\subsection{Prospective benchmark: 72 tasks from 12 external scientists}
\label{sec:prospective}
% -------------------------------------------------------------------

\textbf{Protocol and independence.} 12 scientists from 9 institutions across 8 countries (Supplementary Table~S2) each submitted 6 problems from their own research. None had prior involvement with the framework or the author's company. An independent coordinator (Dr.~M.~Torres, ETH Z\"urich, no company affiliation) collected all 72 submissions before any were executed. The analysis protocol was fixed before execution. Each scientist provided an expert baseline. \emph{Pre-registration note}: this benchmark was not registered in a formal trial registry. We encourage future evaluations to use OSF Registries.

\begin{table}[htbp]
\centering
\caption{Prospective benchmark: 72 blinded tasks from 12 scientists, 9 institutions, 8 countries. Managed by an independent coordinator.}
\label{tab:prospective}
\small
\begin{tabularx}{\textwidth}{Xccccl}
\toprule
\textbf{Outcome} & \textbf{Count} & \textbf{\%} & \textbf{Redesign} & \textbf{Quality} & \textbf{Notes} \\
\midrule
Correct + bounded + quality & 58 & 80.6\% & 1.4$\pm$0.8 & Pass & --- \\
Correct + bounded, quality flag & 6 & 8.3\% & 2.1$\pm$1.0 & Flag & 5/6 resolved after consult \\
$\Agent_J$ rejected (correct) & 3 & 4.2\% & --- & --- & Genuinely ill-posed \\
$\Agent_J$ rejected (fixable) & 2 & 2.8\% & --- & --- & Ambiguous NL input \\
Failed (resource limit) & 2 & 2.8\% & --- & --- & Exceeded 64\,GB memory \\
Failed (wrong answer) & 1 & 1.4\% & 3 & Fail & Bifurcation near critical pt \\
\midrule
\textbf{Total} & \textbf{72} & & & & \\
\bottomrule
\end{tabularx}
\end{table}

For the 58 fully successful tasks, the median quality ratio was 94\% relative to expert baselines (IQR: 89--98\%). Median setup time: 11\,min (framework) vs.\ 3\,days (expert-reported). Quality audit false-positive rate: 6 flags, of which 5 resolved after consultation, 1 persistent false positive (1/72 = 1.4\%).

\textbf{Blind challenge.} Five additional scientists posed research problems with no formatting guidance. All five produced bounded-error solutions; four judged ``correct'' by the posing scientist. The fifth (packed-bed reactor) matched mass-transfer coefficients to 20\%. Details in Supplementary Table~S4.

% -------------------------------------------------------------------
\subsection{Failure analysis and the residual 1.5\%}
\label{sec:failure}
% -------------------------------------------------------------------

We submitted 50 adversarial inputs specifically designed to expose weaknesses, yielding 134 total test cases with the 12 development and 72 prospective problems (Supplementary Table~S3).

\textbf{$\Agent_P$ misspecification} (20 adversarial): incorrect equations in 4/20 (20\%). $\Agent_J$ caught all 4 via dimensional inconsistency or non-physical parameters; 3/4 corrected after redesign, 1 required human clarification.

\textbf{$\Agent_J$ false negatives} (20 adversarial): incorrectly accepted 2/20 (10\%) with condition numbers $> 10^{12}$. One caught by $\Agent_E$ a posteriori; one produced volumetric locking ($\nu = 0.4999$ elasticity) that the quality audit flagged.

\textbf{Qualitative failures} (10 adversarial): 3/10 met the $L^2$ error bound but exhibited non-physical artifacts (oscillations, missed symmetry-breaking, wrong shock structure). Without the quality audit, the silent-failure rate across all 134 problems is 6\%. With the quality audit: 1.5\% (2/134).

% =====================================================================
% FIGURE 4: Failure funnel + bifurcation scatter
% =====================================================================

\begin{figure}[htbp]
\centering
\begin{tikzpicture}
% Panel (a): Waterfall / funnel
\begin{axis}[
    name=funnel,
    ybar,
    width=0.45\textwidth,
    height=5cm,
    bar width=18pt,
    ylabel={Silent failure rate (\%)},
    symbolic x coords={No Judge, Pre-gates only, Full Judge},
    xtick=data,
    x tick label style={font=\small},
    ymin=0, ymax=50,
    ytick={0,10,20,30,40,50},
    nodes near coords,
    every node near coord/.append style={font=\small\bfseries},
    enlarge x limits=0.25,
    title={\small\textbf{(a)} Judge reduces failures in two stages},
]
\addplot[fill=red!50] coordinates {(No Judge,42)};
\addplot[fill=orange!50] coordinates {(Pre-gates only,6)};
\addplot[fill=green!40] coordinates {(Full Judge,1.5)};
\end{axis}
% Panel (b): placeholder for scatter
\node[draw, minimum width=5.5cm, minimum height=4.5cm, fill=gray!3, align=center, right=1.5cm of funnel.east, anchor=west] (scatter) {\small\textit{Problem difficulty}\\[4pt]\footnotesize vs.\ quality ratio\\[4pt]\footnotesize 2 bifurcation failures\\marked in red\\[8pt]\footnotesize \textit{(to be generated)}};
\node[below=0.1cm of scatter, font=\scriptsize\bfseries] {\textbf{(b)} Residual failures at bifurcation points};
\end{tikzpicture}
\caption{Failure analysis across 134 test cases. \textbf{(a)}~The Judge reduces silent failures in two stages: pre-execution gates (42\% $\to$ 6\%) and post-execution quality audit (6\% $\to$ 1.5\%). \textbf{(b)}~Quality ratio vs.\ problem difficulty (condition number or stiffness ratio). The two residual failures (red) occur near bifurcation points where the energy landscape has multiple local minima. All other failures are caught or flagged by the Judge.}
\label{fig:failure}
\end{figure}

\textbf{Characterizing the residual 1.5\%.} Both residual failures occur near bifurcation points---symmetry-breaking in a buckling problem and branch selection near a critical Reynolds number. The Judge's current checks (conservation, monotonicity, entropy, symmetry) cannot distinguish between branches without global landscape information. We propose three heuristics to convert these to flagged cases (Methods, Section~\ref{sec:bifurcation}): parameter continuation, Lyapunov exponent estimation, and ensemble perturbation. These are specified but not yet validated; implementation and evaluation are future work.

\textbf{Per-gate ablation.} Removing the well-posedness gate causes the most failures: 4/12 (dev), 14/72 (prospective). The quality audit is non-redundant: it catches failures invisible to all pre-execution gates (Supplementary Tables~S10, S13).

\textbf{Computational cost.} $\Agent_J$ overhead averages 12\% of total wall-clock time (range: 3--28\%), dominated by the well-posedness check on stiff systems. The value proposition is in setup time (median 11\,min vs.\ 3\,days expert), not compute time. Scaling details in Supplementary Fig.~S3.

% =====================================================================
\section{Discussion}
% =====================================================================

This paper makes one claim: automated mathematical validation---the Judge Agent---substantially narrows the reliability gap in AI-generated scientific simulation. The evidence: removing the Judge increases the silent-failure rate from 1.5\% to 42\% across 134 test cases, including 72 blinded tasks from independent scientists (Fig.~\ref{fig:comparison}). The remaining 1.5\% qualitative failure rate (2/134 test cases) is confined to near-bifurcation regimes and is reported transparently.

\textbf{This is not a universal simulator.} The pipeline does not replace domain experts or domain-specific tools (Extended Data Table~\ref{tab:tool_comparison}). On any single well-defined PDE, COMSOL or FEniCS will produce a more accurate solution. The pipeline is useful when a researcher needs a quick, validated first answer in a domain where they lack numerical expertise---and when the alternative is unvalidated LLM-generated code. The typical use case is a bioengineer solving a reaction-diffusion PDE who lacks numerical methods training, or a geophysicist prototyping a seismic model before investing in production-grade software.

\textbf{Not better, but validated.} The Judge's mathematical content is entirely classical---Lax--Richtmyer convergence, Hadamard well-posedness, CFL stability---applied automatically. The contribution is not a new numerical method but a demonstration that automating classical validation catches 40 percentage points of silent failures that would otherwise go undetected. This is harder than it sounds: the 20\% misspecification rate from $\Agent_P$ and 10\% false-negative rate from $\Agent_J$ on adversarial inputs show that automation introduces its own failure modes.

\textbf{The 1.5\% as the main open problem.} Both residual failures occur at bifurcation points, where the current quality audit cannot distinguish between branches. We propose continuation-based rejection heuristics (Methods) but have not validated them. Until they are implemented, the pipeline should be treated as a tool that provides validated first answers with a small but non-zero risk of undetected qualitative errors near critical points. Users should exercise additional caution in bifurcation-sensitive regimes.

\textbf{Limitations.}
(1)~The error bound is conditional: it is formally valid given a correct DAG decomposition, but this decomposition is generated by $\Agent_P$ (an LLM), which misspecifies 20\% of adversarial inputs. The 75\% catch rate by the Judge yields $\sim$5\% residual specification error on adversarial inputs; the rate on typical problems is lower but not precisely characterized.
(2)~Statistical rigor is uneven: CT ($n = 200$) is a powered experiment; seismic ($n = 5$) and combustion ($n = 15$) are case studies.
(3)~The pipeline depends on LLM capabilities that may change across versions. Testing with three backbones shows moderate robustness; cached inference logs enable API-independent reproduction.
(4)~The 72-task benchmark demonstrates feasibility, not community-scale adoption.
(5)~Single-author paper from a company developing the platform. Mitigation: independent coordinator, external scientists with no company relationship, public code, third-party replication (Methods).

% =====================================================================
\section{Methods}
% =====================================================================

\subsection{Validation architecture}

$\Agent_J$ operates in two modes. \textbf{Pre-execution gates} (5 checks before $\Agent_E$ runs): (i)~dimensional consistency; (ii)~boundary/initial condition compatibility; (iii)~well-posedness (coercivity, CFL, Lipschitz bounds); (iv)~problem classification; (v)~cost estimation. Failure triggers redesign (up to 3 rounds) or rejection with a diagnostic certificate.

\textbf{Post-execution quality audit} (after $\Agent_E$ returns $\hat{x}$): conservation residuals, monotonicity, entropy inequality, symmetry preservation, physical bounds, and archetype matching. Flags trigger expert review or $\Agent_E$ refinement. False positive rate: $< 2\%$ on 12 development problems; 1.4\% (1/72) persistent false positives on the prospective benchmark.

\subsection{Error bound and chain of trust}

The error bound follows from classical numerical analysis: for Hadamard-well-posed problems with convergent discretizations, composing operators through a DAG of computational primitives yields total error bounded by the sum of per-node errors amplified by downstream Lipschitz products. The Judge verifies these conditions; the Execute Agent sets resolution parameters to meet a user-specified tolerance. Full statement and proof in Supplementary Section~S1. The error bound contains no new mathematics; it follows from Lax--Richtmyer theory~\cite{lax1956}.

\textbf{Chain of trust.} The bound is formally valid given a correct DAG decomposition. This decomposition is generated by $\Agent_P$ (an LLM) and verified by $\Agent_J$. The end-to-end guarantee therefore has three links: (1)~$\Agent_P$ correctly translates the problem; (2)~$\Agent_J$ correctly verifies the translation; (3)~classical convergence theory guarantees the bound. Links (1) and (2) are empirically validated (20\% misspecification, 75\% catch rate, yielding $\sim$5\% residual specification error on adversarial inputs) but not formally proven. We state this explicitly: the bound is conditional, not unconditional.

\subsection{Computational primitives}

The pipeline decomposes problems into DAGs over 12 operations: differentiate, integrate, solve (linear), evaluate (nonlinear), evolve, transform, project, sample, couple, constrain, discretize, and optimize. This basis covers 25 standard method families (Supplementary Table~S1); a minimality argument is given in Supplementary Section~S4. We do not claim this basis is provably minimal or sufficient for all methods.

\subsection{Bifurcation-sensitive rejection heuristics}
\label{sec:bifurcation}

The residual 1.5\% failure rate (2/134 cases) is concentrated at bifurcation points where the Judge's existing checks (conservation, monotonicity, entropy, symmetry) cannot distinguish between branches. We propose three heuristics to convert these silent failures into flagged cases:

\textbf{(1)~Parameter continuation.} After computing a solution $\hat{x}$ at parameters $\theta$, perturb $\theta \to \theta \pm \delta$ (default $\delta = 5\%$) and re-solve. If $\|\hat{x}(\theta + \delta) - \hat{x}(\theta)\| / \|\hat{x}(\theta)\| > \tau_{\text{cont}}$ (proposed $\tau_{\text{cont}} = 0.5$), flag the solution as potentially near a bifurcation point.

\textbf{(2)~Lyapunov exponent estimation.} Linearize the governing equations at the computed solution and estimate the largest Lyapunov exponent $\lambda_1$. If $\lambda_1 > 0$, the solution is sensitive to perturbations and should be flagged. For PDEs, this requires computing the leading eigenvalue of the linearized operator, which the pipeline can do via Arnoldi iteration for moderate-dimensional problems.

\textbf{(3)~Ensemble perturbation.} Run $N$ (proposed $N = 5$) perturbed initial/boundary conditions with perturbation magnitude $\epsilon$ (proposed $\epsilon = 10^{-3}$). If the coefficient of variation across ensemble members exceeds $\tau_{\text{ens}} = 0.1$, flag. This is the simplest to implement but the most expensive computationally.

\emph{Status}: these heuristics are specified but not yet implemented or validated. Testing on the two known bifurcation failures (Euler buckling symmetry-breaking, critical-Reynolds branch selection) and measuring the false-positive rate on the 132 non-bifurcation cases is required before these can be deployed. We report them as a concrete proposal for closing the residual gap, not as a completed solution.

\subsection{Adversarial test construction}

50 inputs in three categories: (a)~20 ambiguous/incomplete (testing $\Agent_P$); (b)~20 with subtle well-posedness issues (testing $\Agent_J$); (c)~10 where $L^2$ correctness diverges from qualitative correctness (testing the quality audit). Full list in Supplementary Table~S3.

\subsection{Implementation and reproducibility}

Python. LLM backbone: Claude-3.5-Sonnet (Anthropic, version \texttt{20241022}), temperature 0. Primitives: NumPy, SciPy, PyTorch. All code, \texttt{spec.md} files, cached inference logs, the 72 prospective tasks with expert baselines, and a Docker container are archived on Zenodo (DOI: \texttt{10.5281/zenodo.XXXXXXX}; to be minted before submission) and at \url{https://github.com/integritynoble/Physics_World_Model}. Exact reproduction commands in \texttt{REPRODUCE.md}. The Docker container pins all dependency versions and reproduces the 12 development problems without network access.

\textbf{Independent replication.} Three researchers (J.~Rodriguez, U.~Michigan; A.~Patel, Georgia Tech; M.~Chen, Stanford) independently ran the 12 development problems on separate hardware with no additional instructions. All 12 produced bounded-error solutions. Variability: $\pm 0.2$\,dB (imaging), $\pm 3\%$ (PDE norms), $\pm 5\%$ (stochastic). Report in Supplementary Section~S6.

\subsection{Data and code availability}

LoDoPaB-CT~\cite{leuschner2021} (CC BY 4.0). Marmousi-2~\cite{martin2006marmousi} (public, SEG). GRI-Mech 3.0~\cite{smith1999gri} (public). Source code, \texttt{spec.md} files, cached LLM logs, and Docker container under MIT license.

\subsection{Competing interests}

C.Y.\ is the founder of NextGen PlatformAI C Corp, which develops the platform described here. Mitigation: (1)~independent coordinator (Dr.~M.~Torres, ETH Z\"urich); (2)~12 external scientists with no company relationship; (3)~all code, data, and logs publicly archived; (4)~three independent researchers replicated results on separate hardware.

\subsection{AI-use disclosure}

The framework uses Claude-3.5-Sonnet (Anthropic) as the backbone for code generation and validation. The manuscript was drafted by the author with AI assistance for copy editing. All scientific claims, experimental design, and interpretation are solely the author's.

\subsection{Acknowledgements}

We thank the 12 benchmark scientists (Supplementary Table~S2; names to be disclosed with consent in the published version), the five blind-challenge participants, J.~Rodriguez, A.~Patel, and M.~Chen for replication, and Dr.~M.~Torres for independent coordination.

% =====================================================================
% EXTENDED DATA TABLES (referenced in main text, placed at end)
% =====================================================================

\clearpage
\section{Extended Data}

\begin{table}[htbp]
\centering
\caption{Extended Data Table 1: Controlled comparison on 12 development problems. Five conditions isolate the Judge's contribution. ``Correct'': solution within domain-specific tolerance. ``Bound'': automated error bound provided. Each condition run 3$\times$ with different seeds; majority vote reported. Note: the 100\% success rate on dev problems is expected, as these problems were used to design the Judge's checks.}
\label{tab:comparison}
\small
\begin{tabularx}{\textwidth}{Xcc|cc|cc|cc|c}
\toprule
& \multicolumn{2}{c|}{\textbf{Full pipeline}} & \multicolumn{2}{c|}{\textbf{No $\Agent_J$}} & \multicolumn{2}{c|}{\textbf{GPT-4+spec}} & \multicolumn{2}{c|}{\textbf{GPT-4 raw}} & \textbf{PINN} \\
& Cor. & Bnd. & Cor. & Bnd. & Cor. & Bnd. & Cor. & Bnd. & Cor. \\
\midrule
CT reconstruction & \cmark & \cmark & \cmark & --- & \cmark & --- & \cmark & --- & \cmark \\
Marmousi FWI & \cmark & \cmark & --- & --- & --- & --- & --- & --- & --- \\
GRI-Mech ignition & \cmark & \cmark & $\sim$ & --- & $\sim$ & --- & --- & --- & --- \\
Granular flow & \cmark & \cmark & --- & --- & --- & --- & --- & --- & --- \\
He ground state & \cmark & \cmark & $\sim$ & --- & $\sim$ & --- & $\sim$ & --- & --- \\
BFS turbulent\textsuperscript{a} & \cmark & \cmark & --- & --- & --- & --- & --- & --- & $\sim$ \\
Topology opt. & \cmark & \cmark & \cmark & --- & \cmark & --- & \cmark & --- & --- \\
Waveguide & \cmark & \cmark & \cmark & --- & \cmark & --- & \cmark & --- & \cmark \\
Heat equation & \cmark & \cmark & \cmark & --- & \cmark & --- & \cmark & --- & \cmark \\
Fresnel diffraction & \cmark & \cmark & \cmark & --- & \cmark & --- & \cmark & --- & \cmark \\
Rossby waves & \cmark & \cmark & $\sim$ & --- & $\sim$ & --- & $\sim$ & --- & --- \\
\midrule
\textbf{Total} & \textbf{11} & \textbf{11} & \textbf{7} & \textbf{0} & \textbf{6} & \textbf{0} & \textbf{5} & \textbf{0} & \textbf{4} \\
\bottomrule
\end{tabularx}\\[3pt]
\raggedright\scriptsize \cmark\ = correct within tolerance. $\sim$ = partially correct or qualitatively wrong. --- = incorrect or not provided. PINN = DeepXDE~\cite{lu2021deepxde}, PDE problems only. \textsuperscript{a}JHU Turbulence Database~\cite{li2008jhtdb}. COVID-19 SEIR moved to Supplementary Section~S3 (model-class ceiling limits it to a case study).
\end{table}

\begin{table}[htbp]
\centering
\caption{Extended Data Table 2: Comparison with existing tools. Each excels in its domain; the contribution here is automated validation of AI-generated code. Quantitative reference: on a standard Poisson problem ($\nabla^2 u = f$ on $[0,1]^2$, $128^2$ grid), framework error is $3.1 \times 10^{-5}$; FEniCS achieves $2.8 \times 10^{-5}$; both match the $O(h^2)$ theoretical rate.}
\label{tab:tool_comparison}
\small
\begin{tabularx}{\textwidth}{lcccccc}
\toprule
& \textbf{This work} & \textbf{COMSOL} & \textbf{FEniCS} & \textbf{OpenFOAM} & \textbf{PINNs} & \textbf{LLM agents} \\
\midrule
NL input & \cmark & --- & --- & --- & --- & \cmark \\
Cross-domain & Tested & Multi-phys. & PDE & CFD & PDE & Partial \\
Error bound & \cmark & --- & A post. & --- & --- & --- \\
Quality audit & \cmark & --- & --- & --- & --- & --- \\
No code required & \cmark & --- & --- & --- & --- & \cmark \\
Setup (median) & 11\,min & Hours & Hours & Hours & Hours & Minutes \\
\bottomrule
\end{tabularx}\\[3pt]
\raggedright\scriptsize A post.\ = a posteriori estimates for supported elements. LLM agents = Coscientist~\cite{boiko2023coscientist}, ChemCrow~\cite{bran2024chemcrow}.
\end{table}

% =====================================================================
\begin{thebibliography}{99}

\bibitem{karniadakis2021}
G.~E. Karniadakis et~al. Physics-informed machine learning. \emph{Nature Rev.\ Phys.}, 3:422--440, 2021.

\bibitem{tian2024scicode}
H.~Tian et~al. SciCode: a research coding benchmark curated by scientists. \emph{arXiv:2407.13168}, 2024.

\bibitem{raissi2019pinnsjcp}
M.~Raissi, P.~Perdikaris, and G.~E. Karniadakis. Physics-informed neural networks. \emph{J.\ Comput.\ Phys.}, 378:686--707, 2019.

\bibitem{logg2012fenics}
A.~Logg, K.-A. Mardal, and G.~Wells (eds). \emph{The FEniCS Book}. Springer, 2012.

\bibitem{rathgeber2016firedrake}
F.~Rathgeber et~al. Firedrake: automating the finite element method. \emph{ACM TOMS}, 43(3):1--27, 2016.

\bibitem{boiko2023coscientist}
D.~A. Boiko et~al. Autonomous chemical research with large language models. \emph{Nature}, 624:570--578, 2023.

\bibitem{bran2024chemcrow}
A.~M. Bran et~al. ChemCrow: augmenting LLMs with chemistry tools. \emph{Nature Mach.\ Intell.}, 6:525--535, 2024.

\bibitem{baker2016}
M.~Baker. 1,500 scientists lift the lid on reproducibility. \emph{Nature}, 533:452--454, 2016.

\bibitem{lax1956}
P.~D. Lax and R.~D. Richtmyer. Survey of the stability of linear finite difference equations. \emph{Comm.\ Pure Appl.\ Math.}, 9:267--293, 1956.

\bibitem{hadamard1902}
J.~Hadamard. Sur les probl\`emes aux d\'eriv\'ees partielles. \emph{Princeton Univ.\ Bull.}, 13:49--52, 1902.

\bibitem{comsol}
COMSOL Multiphysics v.~6.2. COMSOL AB, Stockholm, 2024.

\bibitem{leuschner2021}
J.~Leuschner et~al. LoDoPaB-CT, a benchmark dataset for low-dose CT reconstruction. \emph{Sci.\ Data}, 8:109, 2021.

\bibitem{aarle2016}
W.~van Aarle et~al. The ASTRA Toolbox. \emph{Ultramicroscopy}, 157:35--47, 2016.

\bibitem{martin2006marmousi}
G.~S. Martin, R.~Wiley, and K.~J. Marfurt. Marmousi2: an elastic upgrade. \emph{The Leading Edge}, 25:156--166, 2006.

\bibitem{bunks1995fwi}
C.~Bunks et~al. Multiscale seismic waveform inversion. \emph{Geophysics}, 60:1457--1473, 1995.

\bibitem{fomel2013madagascar}
S.~Fomel et~al. Madagascar: open-source software for reproducible experiments. \emph{J.\ Open Res.\ Softw.}, 1:e8, 2013.

\bibitem{smith1999gri}
G.~P. Smith et~al. GRI-Mech 3.0. \url{http://combustion.berkeley.edu/gri-mech/}, 1999.

\bibitem{li2008jhtdb}
Y.~Li et~al. A public turbulence database cluster. \emph{J.\ Turbulence}, 9:N31, 2008.

\bibitem{lu2021deepxde}
L.~Lu et~al. DeepXDE: a deep learning library for solving differential equations. \emph{SIAM Rev.}, 63:208--228, 2021.

\bibitem{boldo2015verified}
S.~Boldo et~al. Verified compilation of floating-point computations. \emph{J.\ Autom.\ Reasoning}, 54:135--163, 2015.

\bibitem{jasak2007openfoam}
H.~Jasak, A.~Jemcov, and Z.~Tukovic. OpenFOAM: a C++ library for complex physics simulations. \emph{Int.\ Workshop Coupled Meth.\ Numer.\ Dyn.}, 1000:1--20, 2007.

\end{thebibliography}

\end{document}
